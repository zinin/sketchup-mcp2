# Post-Iter-2 Direction Decision — Warehouse Resubmit v0.2.0

## TASK

Iter-2 review processing for the warehouse-resubmit v0.2.0 branch is
**fully complete**. Round 1 (commit `644d193`) and round 2 (commit
`795328c`) together applied all 25 AUTO fixes from the iter-2 reviewer
panel and produced the iter-2 decisions log. The plan and design
documents are now in their finalised post-iter-2 state.

The branch is at the «Step 15 of skill» decision point — choose the
direction for the next session and inform the user.

## CRITICAL: DO NOT START WORKING

**STOP. READ THIS CAREFULLY.**

After loading all context below, you MUST:
1. Read the documents and understand the context.
2. Report what you understood (brief summary ≤6 lines).
3. **WAIT for explicit user instructions** before taking ANY action.

**DO NOT:**
- Start implementing the plan's Tasks 1–14.
- Dispatch a new round of reviewers.
- Make any code or document changes.
- Run any commands except reading documents and `git log`/`git status`.

**The user will tell you exactly what to do.** Until then, only read,
summarise, and ask.

## STATE OF THE REPO

**Branch:** `feature/warehouse-resubmit`
**HEAD:** `795328c docs: review iter 2 — round 2 fixes + decisions log (warehouse-resubmit)`

```
795328c  docs: review iter 2 — round 2 fixes + decisions log
644d193  docs: review iter 2 — partial auto-fixes round 1
a0337fc  docs: review iter 1 — decisions + log
2f56d7a  docs: review iter 1 — auto-fixes
ce80b45  docs: review iter 1 — CRITICAL-1 plan tests (sentinel-nil)
fadb746  docs: review iter 1 — partial auto-fixes (design + handoff)
0f7d635  docs(plan): warehouse resubmit implementation plan (v0.2.0)
119da49  docs(spec): warehouse resubmit design (v0.2.0)
```

**Working tree:** clean of tracked-file modifications. The many
untracked files (session-transfer docs, `.gemini/`, `diff.patch`, other
superpowers plans/specs, this prompt file) are pre-existing — **do NOT
stage them.** Use explicit `git add <path>` per iter-1 CONCERN-7 commit
policy if anything new gets created.

## DOCUMENTS

Read in this order:

1. **Design (final post-iter-2 state):** `docs/superpowers/specs/2026-05-28-warehouse-resubmit-design.md`
2. **Plan (final post-iter-2 state):** `docs/superpowers/plans/2026-05-28-warehouse-resubmit.md`
3. **Iter-1 decisions log:** `docs/superpowers/specs/2026-05-28-warehouse-resubmit-review-iter-1.md`
4. **Iter-2 decisions log:** `docs/superpowers/specs/2026-05-28-warehouse-resubmit-review-iter-2.md`
5. **Iter-2 merged reviewer output (input to round 1 & 2):** `docs/superpowers/specs/2026-05-28-warehouse-resubmit-review-merged-iter-2.md`

## PROGRESS

### ✅ Completed (review + planning phase)

- [x] Design v0.2.0 written (`0f7d635`)
- [x] Implementation plan v0.2.0 written (`119da49`)
- [x] Iter-1 review run (codex-gpt-5.5 + glm + albb-deepseek; kimi/qwen failed)
- [x] Iter-1 round 1 — partial auto-fixes (`fadb746`)
- [x] Iter-1 CRITICAL-1 plan tests (`ce80b45`)
- [x] Iter-1 round 2 — remaining auto-fixes (`2f56d7a`)
- [x] Iter-1 decisions log (`a0337fc`)
- [x] Iter-2 review run (codex + albb-deepseek + albb-kimi; glm/qwen failed)
- [x] Iter-2 round 1 — 13 of 25 AUTO fixes (`644d193`)
- [x] Iter-2 round 2 — remaining 12 AUTO via 19 numbered punch-list edits + decisions log (`795328c`)

### ⏸ NOT yet started (implementation phase)

The plan's Tasks 1–14 represent **all the actual code changes**. None
of them have started. The repo's `su_mcp/` tree is still the v0.1.0
post-multi-client-server state, byte-for-byte unchanged. The two review
iterations only touched design.md, plan.md, and review-log files.

- [ ] Task 1: Mechanical rename (filesystem + module + paths)
- [ ] Task 2: Title Case operation labels + silent rescue cleanup
- [ ] Task 3: Logger prefix + default level WARN
- [ ] Task 4: New Config prefs (eval_enabled, log_to_file, log_file_path) + build_profile loading
- [ ] Task 5: Logger writes to file when log_to_file is on
- [ ] Task 6: eval_ruby gate (returns -32010 when disabled)
- [ ] Task 7: Settings validator — accept the new fields
- [ ] Task 8: Settings dialog HTML + Ruby on_save wire-up
- [ ] Task 9: Python eval_ruby — actionable error on -32010
- [ ] Task 10: package.rb — dual-variant build with autogenerated build_profile
- [ ] Task 11: Version bump to 0.2.0
- [ ] Task 12: Documentation updates (README, CLAUDE.md, release.md, cookbook)
- [ ] Task 13: examples/smoke_check.py — graceful skip on -32010
- [ ] Task 14: Final verification

## SESSION CONTEXT (knowledge not in the documents)

### Decision point

User invoked `/continue-plan-fresh-session` immediately after the
iter-2 round-2 final commit (`795328c`). The previous session was about
to ask «iter-3 vs stop-and-execute vs other» via `AskUserQuestion` when
the user interrupted to switch sessions. So the fresh session inherits
the same decision: **what direction to take next.**

### Three plausible directions (do not assume — let user pick)

1. **Stop reviews and begin implementation.** Use either
   `superpowers:executing-plans` (interactive task-by-task) or
   `superpowers:subagent-driven-development` (dispatched subagents per
   task). The plan is 3,070+ lines after both iterations and the tasks
   are sliced to 2–5 min steps — well-suited to either mode. A
   `/do-plan` invocation pauses at a context threshold automatically.

2. **Iter-3 review.** Likely diminishing returns: iter-1 surfaced 32
   issues, iter-2 also surfaced 32. None of the iter-2 fixes touched
   architecture (`§4.1` BuildProfile contract, `§5` Logger structure,
   `§4.3` two-phase confirm flow) — only polish, comments, missing
   edge-case verifications. A third pass would mostly find «nice-to-
   have» suggestions. If the user wants iter-3 anyway, prefer a smaller
   reviewer panel (e.g. codex only + one ccs profile) to cap cost.

3. **Drop docs + open PR ahead of implementation.** Per the user's
   global CLAUDE.md rule, design+plan documents must be `git rm`'d from
   `docs/superpowers/` BEFORE opening a PR. But this is typically done
   **after** Step 14.7 manual SketchUp acceptance — opening a PR
   pre-implementation has no diff to review and would be confusing.

### Reviewer panel notes

- `albb-glm` was skipped per global preference both iterations.
- `glm` (via ccs) failed both times (different upstream issues each time).
- `albb-qwen` failed both times — Alibaba MaaS Anthropic-bridge tenant
  has no Qwen model id; `qwen3.7-max[1m]` and 8 other ids tried all
  return `400 Model not exist`. Don't dispatch albb-qwen again until the
  tenant config is fixed.
- `codex-executor (gpt-5.5, xhigh)` and `ccs-executor / albb-deepseek
  (DeepSeek-V4 Pro, 1M)` worked both times.
- `ccs-executor / albb-kimi` stalled in iter-1, recovered in iter-2
  (~12 min, 23 tool calls).

### Commit policy reminder (iter-1 CONCERN-7)

Always use explicit `git add <path>` — NEVER `git add -A` or `git add
.`. The working tree carries many pre-existing untracked files that
must not land in feature commits. Plan steps reference a placeholder
`git add <explicit-paths> # …` that must be replaced with the actual
file list from each task's Files header.

### Surprise-anchor warning

If applying fixes per any future round and an exact-string anchor in
`Edit` doesn't match — STOP and ask the user. Per iter-1 plan-quality
warning, the plan may have drifted from current document state during
the review cycle. The auto-fix scripts in iter-2 (this commit batch)
applied successfully against all 19 anchors — but iter-3 would target a
different state.

## PLAN QUALITY WARNING

The plan was hardened across 2 review iterations (8 commits totalling
~1,800 lines of design + plan edits) but has NOT yet been executed.
When the user opts for implementation:

- **STOP at the first plan step that doesn't match observed
  codebase state** — the plan was written before any mechanical rename,
  so even Step 1.1 baseline test counts may differ slightly from
  current `230 runs / 516 assertions / 120 passed`.
- **STOP at any sed/find anchor that doesn't apply cleanly** — the
  preamble portability note explicitly says to pick GNU sed (Linux/CI)
  or BSD sed adapt (macOS) per-host; do not blindly run.
- **Do not silently work around plan issues.** Describe the problem,
  explain why the plan doesn't fit, ask the user how to proceed.

## INSTRUCTIONS

1. Read the 5 documents listed in the «DOCUMENTS» section.
2. Read this session's two `git show -s` outputs:
   ```bash
   git show 644d193 -s --format="%H%n%s%n%n%b"  # round-1 disposition list
   git show 795328c -s --format="%H%n%s%n%n%b"  # round-2 disposition list
   ```
3. Summarise current state in ≤6 lines.
4. **STOP and WAIT** — do NOT start anything.
5. Ask the user via `AskUserQuestion` the same Step-15-of-skill
   question the previous session was about to ask:
   «Что дальше по ветке warehouse-resubmit?»
   - Stop reviews and begin implementation (via `/do-plan`,
     `superpowers:executing-plans`, or `superpowers:subagent-driven-development`)
   - Iter-3 review (warn about diminishing returns)
   - Other
