# Continuation prompt — кровати 90×200, после сессии «Материал и раскрой Bed_D» (2026-07-12)

Скопируй всё ниже в новую сессию Claude Code.

---

## TASK

Продолжить работу над моделью 4 кроватей 90×200 в живом SketchUp (ветка `feature/bed-90x200`).

Прошедшая сессия «Материал и раскрой Bed_D» была **READ-ONLY**: модель НЕ менялась (только зонды и скриншот-состояние прежнее). Посчитан материал на Bed_D (корпус + ящики), составлены карты раскроя щитов 2000×600 и фанеры 12 для 1/2/3 одинаковых кроватей (`docs/bed-d-cutting-layouts.md`), **всё закоммичено в git** по явной просьбе пользователя. Плана следующей фазы НЕТ — **фазу назовёт пользователь**.

Режим работы: **inline** (правило ledger — субагентов и мульти-агентные раны НЕ запускать без явной просьбы).

## CRITICAL: DO NOT START WORKING

**STOP. READ THIS CAREFULLY.**

После загрузки контекста:
1. Прочитай документы (ledger — ЦЕЛИКОМ)
2. Кратко изложи, что понял (состояние модели, инварианты, правила)
3. **ЖДИ явных указаний пользователя** — никаких действий до них

**НЕЛЬЗЯ:**
- начинать какие-либо задачи или правки модели
- менять код/файлы
- запускать команды (кроме чтения документов)
- угадывать, какая фаза следующая

## DOCUMENTS (читать в этом порядке)

- Progress ledger — ЦЕЛИКОМ (летопись модели и все технические грабли, вне git): `.superpowers/sdd/progress.md` — включая записи «Расчёт материала Bed_D 2026-07-12» с дополнениями по раскрою и handoff (5)
- Итог прошедшей сессии — карты раскроя: `docs/bed-d-cutting-layouts.md`
- План закрытой фазы ящиков (тримлен до ✅-строк): `docs/superpowers/plans/2026-07-12-bed-drawers-rebuild.md`; полный текст при надобности: `git show f888333:docs/superpowers/plans/2026-07-12-bed-drawers-rebuild.md`
- Спека конструкции ящиков (актуальное описание тел): `docs/superpowers/specs/2026-07-12-bed-drawers-rebuild-design.md`
- Спека присадки Bed_D (крепёж корпуса: минификсы, шканты 8, стяжки): `docs/superpowers/specs/2026-07-12-bed-d-dowel-cam-drilling-design.md`
- По надобности: `docs/sketchup-ruby-cookbook.md`

## PROGRESS

**Завершено (вся хроника — в ledger):** постройка Bed_A/Bed_B (Tasks 1–12 + Правки 1–4), постройка модульных Bed_C/Bed_D, перевод ящиков на боковые шариковые SETE SB-45750 + присадка направляющих (Slides Tasks 1–7), присадка Bed_D под шканты 8/минификсы (Cam Tasks 1–8), пересборка всех 10 ящиков под дно 12 + шканты 6×30 (Drawer Tasks 1–7), расчёт материала Bed_D + карты раскроя на 1/2/3 кровати.

**Осталось:** ничего запланированного. Кандидаты следующей фазы (решает пользователь):
- пересчёт закупки §9 для остальных кроватей (Bed_D посчитана полностью; A/B/C — нет; амендменты прошлых фаз перечислены в ledger)
- судьба Bed_A/Bed_B рядом с модульными C/D (удаление — только по явному подтверждению)
- детализация упрощённых тел направляющих; допроверка 12,5 vs 12,7 и нагрузки пары SB-45750 на 750 мм (сырьё в `.superpowers/sdd/2026-07-10-research-750mm-raw.md` БЕЗ верификации)
- финализация ветки (⚠ перед PR: `git rm -r docs/superpowers/` — правило пользователя; каталог теперь ТРЕКАЕТСЯ, см. ниже)
- ручки фасадов — вне скоупа модели (сверлятся по месту)

## SESSION CONTEXT (сессия «Материал и раскрой», 2026-07-12)

Состояние модели — НЕ менялось с прошлого handoff (сохранена пользователем после фазы ящиков):
- Все 10 коробок: дно 12 накладное снизу (Z 40–52, наружный габарит), стенки 228 @ Z 52–280, боковины перекрывают перед/зад. Шканты 6×30: оси Y 932/194, Z 90/130/240.
- Присадка: «Присадка ящиков» 240 вырезов Ø6 + 648 точек (120/120/204/204); «Присадка D» 222 выреза + 310 точек на 8 тегах; тег «Присадка» = 40 cline + 160 cpoint (Z 160,5).
- Инварианты: **233 группы** (truncated=false), **20 слоёв**; узловые bbox 10 ящиков — в ledger (Drawer Task 1).
- Выдвинуты +350 по Y: A.drawer1, B.drawer2, C.drawer1, D.drawer2. Выделение в SketchUp на конец сессии: **Bed_D**.
- Видимость тегов: Layer0/Корпус/Присадка + 8 тегов «Присадки D» + 4 тега «Присадки ящиков» = on; Матрасы/Ламели/Направляющие/Ящики/Фасады ящиков = off. Камера — общий вид C+D.

Итоги расчёта (детали в ledger и `docs/bed-d-cutting-layouts.md`):
- Материал одной Bed_D: щит 18 нетто 4,57 м²; фанера 12 нетто 3,25 м²; брусок 40×40 3,9 м; ламели 90×20 11,6 м; крепёж корпуса (спека D §3): минификс 30, шкант 8×30 48 / 8×25 10, стяжки 8, шурупы 4×50 ×9 / 4×45 ×9 / 4×35 ×26; ящики: SB-45750 3 пары, шкант 6×30 36, шурупы 4×40 42 / 4×25 12.
- Раскрой щитов 2000×600: **6 / 11 / 16** на 1/2/3 кровати (закономерность 5N+1, минимумы доказаны перебором паттернов); фанера 1525×1525: **2 / 4 / 5** листов (на 4-м листе двух кроватей всего 2 детали). Тесная посадка: пары 750+750 в ряду листа — запас 21 мм (пропил ≤ ~10 мм).
- **Вариант Б** (идея пользователя, добавлен в файл раскроя коммитом `500c0d5`): задние стенки, Γ-стенки и планки пола — из фанеры 18 (не видны) → щитов всего **2 / 4 / 6**; фанера 18: **2 / 3 / 5** листов 1525×1525 или **2 / 3 / 4** листа 2440×1220 (на 3 кровати большой формат выгоднее); фанера 12 ящиков без изменений. Пометки: торец передней планки в 16-мм щели под фасадом станет слоистым (утоплен на 18); допуск толщины фанеры чуть расширит камеры.
- Оговорки: минификс B34 — типовой, сверить с купленной фурнитурой; нагрузка пары SB-45750 на 750 мм НЕ верифицирована.

Git этой сессии (ветка `feature/bed-90x200`):
- `564925c` chore: `.gitignore` + `.venv.*` (мусорный `.venv.broken-task8/` скрыт из статуса, НЕ удалён с диска)
- `17aab6b` docs: 71 файл — `bed-d-cutting-layouts.md`, `bed-drawer-slides-research.md` и ВСЕ накопленные `docs/superpowers/` промпты/спеки
- этот continuation prompt тоже закоммичен (конвенция «промпты untracked» отменена явной просьбой пользователя «закоммить всё»)
- `500c0d5` docs: вариант Б раскроя (скрытые детали из фанеры 18) + обновление этого промпта следом
- ⚠ Следствие: перед PR обязательно `git rm -r docs/superpowers/` + коммит (правило пользователя — плановые доки не должны попасть в PR diff)

Правила работы (обязательны, источник — ledger):
- Общение по-русски; пользователь бережёт токены — короткие отчёты, но ledger вести подробно.
- Перед любыми мутациями модели — preflight-зонд (`get_version`, `list_components` recursive == 233 / truncated=false, точечные bbox-зонды). Пользователь работает в SketchUp сам — модель могла измениться; при любом расхождении СТОП и спросить.
- Мутирующий `eval_ruby` — самодостаточный чанк: ровно одна операция `start_operation` → `commit_operation`, `abort_operation` в rescue, JSON-отчёт последним выражением (`puts` не виден). `ok:false` откатывается сам; MCP `undo` — ровно один, только для закоммиченного, но не прошедшего сверку чанка.
- Порог сверок bounds/bbox — 0,2 мм; объёмы ±5 мм³; объём круглого выреза — по 24-сегментному полигону, не π·r².
- Solid-tools ЗАПРЕЩЕНЫ (пересоздают группу — гибнут guide-точки); резать только `add_circle` + `pushpull` внутри `definition.entities`. Врезной `pushpull` уничтожает исходный Face — пол искать заново геометрически.
- Перед мутацией узла — assert «накопленный трансформ = чистая трансляция»; bbox сверять в МИРОВЫХ координатах (definition.bounds × накопленный трансформ).
- `model.save` НЕ вызывать — сохраняет пользователь. Видимость тегов не трогать вне операций с возвратом.
- Git менять только в рамках конвенций (доки/handoff); перед PR — `git rm` каталога `docs/superpowers/`.

## PLAN QUALITY WARNING

Плана на следующую фазу НЕТ — не выдумывай его. Документы могли разойтись с живой моделью (пользователь работает в SketchUp сам): любые координаты/счётчики перед использованием сверять зондом; при расхождении с ledger/спекой — СТОП, описать, спросить.

## INSTRUCTIONS

1. Прочитай документы в указанном порядке (ledger — целиком)
2. Кратко изложи понимание: состояние модели, инварианты, правила
3. **СТОП и ЖДИ** — спроси: «Какую фазу берём?»
