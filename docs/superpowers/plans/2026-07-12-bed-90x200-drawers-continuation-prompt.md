## TASK

Новая фаза работы над SketchUp-моделью кроватей 90×200 (репозиторий sketchup-mcp2, ветка `feature/bed-90x200`): **ящики**. У пользователя есть вопросы по ним — конкретику он озвучит сам в начале сессии; готового плана/спеки на эту фазу НЕТ.

Предыдущая фаза «Присадка Bed_D под шканты 8 мм и минификсы 15/B34» завершена целиком (ledger, записи «Cam Task 1..8»); модель сохранена пользователем на диск 2026-07-12.

## CRITICAL: DO NOT START WORKING

**СТОП. Сначала контекст, потом — только по команде.**

После чтения документов:
1. Подтверди, что контекст загружен
2. Кратко изложи понимание (состояние модели, что за ящики, известные открытые темы)
3. **ЖДИ вопросов/указаний пользователя** — никаких действий до них

**НЕЛЬЗЯ до явной команды:** менять модель или репозиторий, запускать мутирующие `eval_ruby`, начинать дизайн/план. Вопросы открытые — для творческой части сначала superpowers:brainstorming, дизайн-док коммитить только на feature-ветку (правило из глобального CLAUDE.md).

## DOCUMENTS (читать в этом порядке)

- Progress ledger — ЦЕЛИКОМ, летопись модели и ВСЕ технические грабли (вне git): `.superpowers/sdd/progress.md`
- Исследование ящиков/направляющих: `docs/bed-drawer-slides-research.md` (верифицированная часть 1 + чек-лист правок §6; сырьё части 2 БЕЗ верификации: `.superpowers/sdd/2026-07-10-research-750mm-raw.md` — факты оттуда перед использованием проверять точечно)
- Рецепты SketchUp Ruby API: `docs/sketchup-ruby-cookbook.md` (по надобности)
- Для справки — завершённая фаза присадки D: спека `docs/superpowers/specs/2026-07-12-bed-d-dowel-cam-drilling-design.md`; план тримлен, полный — `git show 7d89fb4:docs/superpowers/plans/2026-07-12-bed-d-dowel-cam-drilling.md`

## СОСТОЯНИЕ МОДЕЛИ (сохранена 2026-07-12; 233 группы, 16 слоёв)

- 4 кровати: Bed_A, Bed_B (цельные), Bed_C (2 модуля), Bed_D (3 модуля, X 7968–10024).
- Ящики (фанера 12, дно 6):
  - **A.drawer2** — перестроен под боковые шариковые SETE SB-45750 (ledger «Slides Task 2»): коробка 976, дно 962×736×6 @ Z 53–59, рельсы 12,5/сторону на стенках камеры Z 138–183.
  - **A.drawer1, B.drawer1..3** — СТАРАЯ конструкция «скрытый монтаж» (дно в пазу, направляющие под дном) — НЕ перестраивались.
  - **C.drawer1..2** (наружная ширина 967), **D.drawer1..3** (625/623/625) — построены под боковые шариковые сразу; C.drawer1 и D.drawer2 выдвинуты +350.
- Присадка: тег «Присадка» — осевые направляющих, 40 линий + 160 точек на всех 4 кроватях (стенки камер И боковины ящиков); папка «Присадка D» (8 тегов «сверло×глубина») — 222 выреза + 310 точек в корпусе Bed_D.
- Видимость на момент сохранения: Layer0/Корпус/Присадка + 8 тегов папки «Присадка D» on; Матрасы/Ламели/Направляющие/Ящики/Фасады ящиков off.

## ИЗВЕСТНЫЕ ОТКРЫТЫЕ ТЕМЫ ВОКРУГ ЯЩИКОВ (из ledger; что из этого — вопросы пользователя, он скажет сам)

- A.drawer1 и B.drawer1..3 всё ещё «скрытый монтаж» — перестраивать ли по образцу A.drawer2; судьба Bed_A/Bed_B рядом с модульными C/D — решение пользователя, удаление чего-либо только по явному подтверждению.
- Дно 6 мм в пазу при 30–40 кг — открытый вопрос исследования (для боковых шариковых менее критично, чем для скрытого монтажа, где дно несущее).
- Присадка ящиков/фасадов НЕ делалась — осознанный скоуп фазы присадки D (фанера 12 не несёт минификс 15×13; шкант 8 оставляет 2 мм стенки). Захочет присадку ящиков — крепёж другой (шурупы/паз/конфирматы) и новый brainstorm.
- Детали из исследования про заднюю стенку/замки (вырез ≥35×13, Ø6×10, замки L/R) относятся к СКРЫТОМУ монтажу — на боковые шариковые напрямую не переносить.
- Детализация упрощённых тел направляющих; точечная допроверка 12,5 vs 12,7 и нагрузки 45-мм пары на 750 мм (сырьё без верификации).
- Пересчёт закупки §9 под модульные C/D — не делался.

## SESSION CONTEXT — правила работы (ОБЯЗАТЕЛЬНЫ, источник — ledger)

- Общение по-русски; пользователь бережёт токены — короткие отчёты, но ledger вести подробно (запись после каждого таска/правки).
- Режим исполнения планов: **inline** (superpowers:executing-plans); субагентов и мульти-агентные раны НЕ запускать без явной просьбы.
- Перед работой с моделью — preflight: `get_version` (совместимость), `list_components` recursive (233 группы, truncated=false) — сверить, что модель та самая.
- Мутирующий `eval_ruby` — самодостаточный чанк: ровно одна операция `start_operation` → `commit_operation`, `abort_operation` в rescue, JSON-отчёт последним выражением (`puts` не виден). `ok:false` откатывается сам; MCP `undo` — ровно один, только для закоммиченного, но не прошедшего сверку чанка.
- **ВРЕЗНОЙ `face.pushpull` (снятие материала) УНИЧТОЖАЕТ исходный Face** — после него грань дна искать заново геометрически (грань ∥ оси на глубине t, `classify_point` центра оси == PointInside; готовый `drill` — в полном плане присадки и ledger «Cam Task 3»). Ссылка живёт только при вытягивании наружу.
- Перед мутацией детали — assert «накопленный трансформ bed×node×деталь = чистая трансляция»; bbox сверять в МИРОВЫХ координатах (definition.bounds × трансформ), не голым Group#bounds.
- Порог самопроверок bounds — **0,2 мм** (1e-6″ ложно срабатывает на шум ленивого пересчёта bbox).
- Объёмы `Group#volume` — кубические дюймы, переводить ×25.4³.
- Solid-tools ЗАПРЕЩЕНЫ на деталях с guide-геометрией (пересоздают группу — погибнет присадка внутри definition); резать только add_circle+pushpull внутри definition.entities.
- ⚠ Пересборка ящика УБЬЁТ разметку «Присадка» на его боковинах (она внутри definition боковин; entity ID сменятся) — переносить/пересоздавать разметку осознанно, как делалось в Slides Task 2→3.
- `model.save` НЕ вызывать — сохраняет пользователь. Существующие теги/видимость не трогать без операции с возвратом.
- ⚠ B34 минификса — типовой размер, перед реальным сверлением сверить с купленной фурнитурой (дисклеймер спеки §2.2, повторно не обсуждать).

## PLAN QUALITY WARNING

Документы и ledger могли разойтись с живой моделью (пользователь работает в SketchUp сам). При любом расхождении данных preflight/зондов с ожиданиями: СТОП, описать расхождение, спросить пользователя. НЕ обходить проблемы молча и НЕ отклоняться от согласованных решений без одобрения.

## INSTRUCTIONS

1. Прочитай документы по списку (ledger — целиком)
2. Кратко изложи понимание
3. **СТОП и ЖДИ** — спроси: «Какие вопросы по ящикам разберём?»
