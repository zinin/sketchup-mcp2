## TASK

Execute the implementation plan for **warehouse resubmission v0.2.0** (Extension Warehouse rejected the v0.1.0 submission; spec + plan addressing every blocker have been written and committed).

Use `/superpowers:subagent-driven-development` skill for execution. Recommended invocation: `/do-plan` (it will spawn fresh subagents per task with two-stage review and pause at the configured context threshold).

## DOCUMENTS

- Design: `docs/superpowers/specs/2026-05-28-warehouse-resubmit-design.md`
- Plan: `docs/superpowers/plans/2026-05-28-warehouse-resubmit.md`

Read both documents first. The plan has 14 tasks; the spec has 13 sections that the plan's self-review (last section) maps 1:1.

## IMPORTANT: DO NOT START WORK YET

After reading the documents:
1. Confirm you have loaded all context.
2. Summarize your understanding briefly — at minimum: the rename target, the two build variants, the `-32010` contract, and which tasks are mechanical-only vs feature work.
3. **WAIT for user instruction before taking any action.**

Do NOT begin implementation until the user explicitly tells you to start.

## SESSION CONTEXT (decisions and reasoning that the docs don't fully spell out)

### Current state of the repo

- Branch: `feature/warehouse-resubmit` (already checked out; created via `git switch -c`).
- Two commits already on the branch:
  - `119da49` — `docs(spec): warehouse resubmit design (v0.2.0)`
  - `0f7d635` — `docs(plan): warehouse resubmit implementation plan (v0.2.0)`
- Master is at `66103ff` (untouched).
- All untracked files visible in `git status` are pre-existing (session-transfer docs, `.gemini/` config, `diff.patch`) — leave them alone.
- Working tree is otherwise clean.
- Baseline test counts: Ruby `230 runs, 516 assertions`; Python `120 passed`.

### Key brainstorming decisions and rationale

1. **Naming: «MCP Server for SketchUp» + filesystem prefix `mcp_for_sketchup`**
   User explicitly picked the *neutral* form over the «Zinin MCP Server» variant — chose the reviewer-suggested clean form. Module name is `MCPforSketchUp` (CamelCase, valid Ruby constant). Preference SECTION is `"MCPforSketchUp"`. Trimble product_id changes from `SU_MCP_SERVER` to `MCP_FOR_SKETCHUP` (treated as a new product, not an update).

2. **eval_ruby strategy: disabled-by-default + Settings toggle with warning, no per-call confirmation**
   User explicitly rejected per-call confirmation (would break LLM-driven multi-step workflows that the cookbook depends on). User added TWO requirements beyond the rejected reviewer text:
   - **Python wrapper must convert `-32010` into actionable text** (not a raised exception) so the LLM can repeat the «open Settings and enable…» hint verbatim to the user. Baked into spec §4.4 and Task 9.
   - **Two build variants from same source:** `--variant=warehouse` (eval off by default; for Extension Warehouse) and `--variant=github` (eval on by default; for GitHub Releases, self-signed via Trimble). Baked into spec §4.1 / §8 and Task 10. Both `.rbz` go through Trimble signing service — the only differentiator is one constant baked into a per-build autogenerated `core/build_profile.rb`.

3. **Logging: default WARN + console prefix `[MCPforSU]` + optional log-to-file in Settings**
   User explicitly picked the «all three» option (combined approach). File output is `Dir.tmpdir/mcp_for_sketchup.log` by default (cross-platform; no Documents-folder permission issues). The «Show Log» menu opens the file when log-to-file is on, otherwise opens `SKETCHUP_CONSOLE`.

4. **No migration code.** User explicitly confirmed: «считаем, что кроме нас никто не пользуется пока» — treat v0.1.0 as never-shipped. SketchUp preferences SECTION rename causes a one-time settings reset on the dev machine; acceptable. MIN_PYTHON/MIN_RUBY both bumped to `0.2.0` — wire-protocol break is intentional.

5. **Version: 0.2.0 on both sides.** User confirmed bump to 0.2.0 in both halves.

### Rejected alternatives (don't re-litigate these unless the user reopens them)

- **Removing `eval_ruby` entirely** — would break the cookbook recipes and `examples/arts_and_crafts_cabinet.py`. Rejected by user.
- **Per-call confirmation dialog for `eval_ruby`** — would break LLM workflows. Rejected by user.
- **Renaming PyPI package `sketchup-mcp2`** — out of warehouse review scope; would break PyPI continuity. Stays.
- **Renaming Python module `sketchup_mcp`** — not reviewer-flagged. Stays.
- **Migrating `SU_MCP` prefs → `MCPforSketchUp` prefs** — no real users. Skipped.
- **Branded extension name «Zinin MCP Server for SketchUp» / prefix `zmcp`** — user chose neutral form instead.
- **Dedicated log file under `~/Documents/...`** — picked `Dir.tmpdir` for simplicity (no mkdir-in-Documents permission edge cases).

### Out of scope (deliberately)

- Tool API renames — wire protocol is stable; reviewer had no complaints there.
- Removing `eval_ruby` from `dispatch.rb` — only the handler is gated; the dispatch route stays.
- Menu label «MCP Server» (under Plugins menu) — reviewer's complaint was the *extension name*, not the menu label. Stays.
- Refactoring `Group#subtract` reversed semantics — that's an upstream SketchUp Ruby API quirk documented in CLAUDE.md and the cookbook. Do not touch.

## EDGE CASES & PLAN-EXECUTION WARNINGS

These are caveats noticed during plan writing that the task descriptions themselves don't fully highlight:

1. **GNU sed assumption.** The plan uses `sed -i 's/.../.../g'` syntax (GNU). On macOS, sed needs `-i ''` for in-place edits. Project is developed on Linux per the env, so this should be fine — but if the executor is on macOS, adjust syntax.

2. **Task 1.4 sed pass over-renames the SECTION literal.** The bare regex `SU_MCP` matches both the Ruby module AND the `"SU_MCP"` string literal in `config.rb`. Step 1.5 explicitly verifies the SECTION value lands at `"MCPforSketchUp"` — that's what we want. If you ever need both (module and literal differing), you'll need a more targeted sed.

3. **`build_profile.rb` is gitignored.** Tests run *without* it. `Config.eval_enabled?` falls back to `false` when neither runtime pref nor the build_profile constant is set — this is the safer warehouse-default behavior baked into Task 4. Don't try to commit the autogenerated file.

4. **server.rb line numbers in Step 2.11 are pre-rename.** The Task 1 sed pass may shift line numbers slightly. Use `grep -n` to re-locate each silent rescue if the literal line numbers don't match exactly.

5. **Step 13.3 wrap counts.** The plan estimates «≤ 3 eval_ruby call sites» in `examples/smoke_check.py` based on description. Verify by `grep -n eval_ruby examples/smoke_check.py` first; wrap each.

6. **Step 14.7 is manual.** Final SketchUp verification cannot be done in headless / agentic mode. Treat as a release checklist that the user runs on a machine with SketchUp 2026+ installed.

7. **Don't remove design+plan docs before Step 14.7.** Per user's global CLAUDE.md, design/plan docs are removed via `git rm` only *before* opening a PR, not before merging or executing tasks. Step 14.9 covers this explicitly; don't anticipate.

8. **No `git push --force` ever.** Wire protocol is breaking but the *branch* is new — only `git push -u origin feature/warehouse-resubmit` is needed (Step 14.8). Do not force-push.

## PLAN QUALITY WARNING

The plan was written for a large task and may contain:
- Errors or inaccuracies in implementation details (line numbers, regex patterns, exact string spellings)
- Oversights about edge cases or dependencies between Ruby files
- Assumptions that don't match the actual codebase shape after Task 1's mechanical rename
- Missing steps or incomplete instructions (especially around the HTML/JS dialog wiring in Task 8)

**If you notice any issues during implementation:**
1. STOP before proceeding with the problematic step.
2. Clearly describe the problem you found (which file, which line, what was expected vs what is present).
3. Explain why the plan doesn't work or seems incorrect.
4. Ask the user how to proceed.

Do NOT silently work around plan issues or make significant deviations without user approval.

## VERIFICATION SHORTCUTS

At any point during execution, you can sanity-check progress by:

```bash
# All Ruby tests
ruby test/run_all.rb 2>&1 | tail -5

# All Python tests
uv run pytest tests/ -q 2>&1 | tail -5

# No legacy prefix anywhere in code/tests/docs
git grep -in 'su_mcp\|SU_MCP\|Sketchup MCP Server' \
  -- ':!docs/superpowers/specs/*' \
     ':!docs/superpowers/plans/*' \
     ':!docs/session-transfer-*' \
     ':!CHANGELOG*' ':!*.lock'

# Build both variants and verify they differ
cd mcp_for_sketchup && ruby package.rb --variant=warehouse && ruby package.rb --variant=github && cd ..
unzip -p mcp_for_sketchup/*-warehouse.rbz mcp_for_sketchup/core/build_profile.rb
unzip -p mcp_for_sketchup/*-github.rbz    mcp_for_sketchup/core/build_profile.rb
```
