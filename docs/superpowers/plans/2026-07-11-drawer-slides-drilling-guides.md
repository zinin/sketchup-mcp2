# Присадка направляющих SETE SB-45750 — implementation-план

**Статус: ✅ ИСПОЛНЕН ЦЕЛИКОМ (2026-07-12).** Все 7 тасков выполнены и приняты; подробности по каждому — ledger `.superpowers/sdd/progress.md`, записи «Slides Task 1..7». Полный текст плана: `git show 7791ff7:docs/superpowers/plans/2026-07-11-drawer-slides-drilling-guides.md`.

**Goal:** В живой модели SketchUp перестроить `A.drawer2` под боковые шариковые направляющие и нанести присадку (guide-линии + guide-точки, тег «Присадка») на все 10 ящиков четырёх кроватей по спеке `docs/superpowers/specs/2026-07-11-drawer-slides-drilling-guides-design.md`.

**Итог исполнения:** 40 guide-линий + 160 guide-точек на теге «Присадка» (A 8+32, B 12+48, C 8+32, D 12+48), все мировые координаты — отклонение 0.0 от Expected; 233 группы (инвариант держался после каждого чанка); bbox узлов не менялись; git исполнение не меняло; модель сохранена пользователем.

## Global Constraints

✅ Соблюдены (полный текст — в git-версии `7791ff7`). Согласованное изменение: порог b0/b1-самопроверки `mark_part` 1e-6″ → **0,2 мм** (0.2/25.4) — ложное срабатывание исходного порога на разовый шум ленивого пересчёта bbox SketchUp сразу после большой операции; дебаг задокументирован в ledger «Slides Task 3».

## Прелюдия-A (строительная — только Task 2)

✅ Исполнена (текст — git `7791ff7`). Согласованное изменение: в `report` строка узла — `gbox.call(node.definition)` вместо `gbox.call(node)` (мировые координаты; валидно, т.к. assert гарантирует bed×node = identity; голый `Group#bounds` узла живёт в системе Bed_A, +18 по X).

## Прелюдия-B (присадочная — Tasks 3–6) и Исполнительный блок присадки

✅ Исполнены (текст — git `7791ff7`) с порогом самопроверки 0,2 мм.

---

### Task 1: Preflight — соединение, снимок модели, готовность

✅ Done — ledger «Slides Task 1»: версии 0.3.0↔0.3.0 compatible; 7 тегов, «Присадки» нет; 233 группы, truncated=false; все 17 стенок-носителей, 9 пар боковин и старый A.drawer2 — 0.0; eval-гейт ok.

### Task 2: Перестройка `A.drawer2` под боковые шариковые

✅ Done — ledger «Slides Task 2»: 7 старых деталей → 7 новых, все 9 bbox точно (0.0); коробка 976 (X 1049,5–2025,5), дно 962×736×6 @ Z 53–59, рельсы 12,5 на стенках камеры Z 138–183; фасад и мировой bbox узла [1030,188,16,2052,956,286] не менялись.

### Task 3: Присадка Bed_A

✅ Done — ledger «Slides Task 3»: 8 линий + 32 точки, 0.0; тег «Присадка» создан. (Первый запуск — ложный провал самопроверки bounds; корневая причина и решение — см. Global Constraints выше.)

### Task 4: Присадка Bed_B

✅ Done — ledger «Slides Task 4»: 12 линий + 48 точек, 0.0.

### Task 5: Присадка Bed_C

✅ Done — ledger «Slides Task 5»: 8 линий + 32 точки, 0.0.

### Task 6: Присадка Bed_D

✅ Done — ledger «Slides Task 6»: 12 линий + 48 точек, 0.0. Общий итог по модели сверен суммой отчётов: **40 линий + 160 точек**.

### Task 7: Скриншоты и визуальная приёмка

✅ Done — ledger «Slides Task 7»: кадр 1 (сцена, 4 кровати) и кадр 2 (голый корпус Bed_A с разметкой) сняты, чек-лист (а)/(б)/(в) пройден; видимость возвращена точно по before, «Присадка» оставлена ON, HideConstructionGeometry=false; модель сохранена пользователем.
