## TASK

Исполнить план фазы «Пересборка ящиков: дно 12 мм, углы на шкантах 6×30» в живой SketchUp-модели кроватей 90×200 (ветка `feature/bed-90x200`).

Режим исполнения: **inline** — скилл `superpowers:executing-plans` (выбор пользователя; субагентов и мульти-агентные раны НЕ запускать без явной просьбы — правило ledger).

## DOCUMENTS (читать в этом порядке)

- Progress ledger — ЦЕЛИКОМ (летопись модели и все технические грабли, вне git): `.superpowers/sdd/progress.md`
- Спека: `docs/superpowers/specs/2026-07-12-bed-drawers-rebuild-design.md`
- План: `docs/superpowers/plans/2026-07-12-bed-drawers-rebuild.md` (7 тасков; Прелюдия-R и RUN-блок — в «Справочнике фазы» плана)
- По надобности: `docs/sketchup-ruby-cookbook.md`

## IMPORTANT: DO NOT START WORK YET

После чтения документов:
1. Подтверди, что контекст загружен
2. Кратко изложи понимание (что перестраиваем, какими чанками, какие инварианты)
3. **ЖДИ явной команды пользователя** — никаких действий (включая read-only зонды) до неё

## SESSION CONTEXT (не в документах — из сессии brainstorm/планирования 2026-07-12)

Решения пользователя и отвергнутые альтернативы:
- Дно 12 **накладное снизу** (= наружный габарит коробки, стенки встают на него). Отклонены: вкладное на шурупах, паз 12×6 (ослабляет стенку до 6 мм, 40 пазов фрезеровки).
- Углы — **шканты 6×30 + ПВА, 3 на угол** (у пользователя есть шканты 6 и кондуктор). Отклонены: шурупы через боковину, конфирматы (Ø7 в торце фанеры 12 — расслоение), шкант 8 (2 мм стенки — отвергнут ещё в фазе присадки D).
- Фасад — **жёстко, шурупы 4×25 изнутри, без регулировки** (отклонена регулировка через Ø8). ⚠ 4×30 нельзя: 12+18=30 — пробьёт фасад насквозь.
- Ручки фасадов — вне скоупа (сверлятся по месту под купленную фурнитуру).
- Скоуп: **все 10 ящиков** (A×2, B×3, C×2, D×3), тела + полная присадка (вырезы + гайд-точки, по образцу фазы «Присадка D»).
- Вертикальная посадка: дно внутрь габарита (Z 40–52, стенки 52–280) — габарит коробки и зазор 22 мм над планками-стяжками пола сохраняются; вариант «дно вниз, стенки 240» отклонён (зазор упал бы до 10 мм).

Неочевидное, проверенное в сессии планирования:
- `front` — деталь у фасада (Y 926–938 закрыто), `back` — у стены (Y 188–200); подтверждено планом постройки (`git show 4b7be1b`). В первой редакции спеки подписи были перепутаны — исправлено коммитом `fbb7c49`.
- Имена новых тегов — с суффиксом «ящика» (`Ø3 пилот` уже занят папкой «Присадка D»; теги SketchUp глобальны).
- B.drawer3: X 4044.5–4680.5 (камера 662, зазоры 12.5+13.5 — «1 мм воздуха» задуман, из плана постройки).
- Выдвинутые ящики (A.drawer1, B.drawer2, C.drawer1, D.drawer2, все +350 по Y) — Expected-таблицы плана УЖЕ сдвинуты; чанк строит в локальных координатах через обратный трансформ.
- Шканты Z 90/130/240 намеренно неравномерны: полоса направляющей Z 138–183 на боковине оставлена пустой (шурупы планки SETE сверлятся по месту), шуруп дна в стенке ≤ Z 82 — матрица разведения; координаты без пересчёта не менять.
- Объём выреза считать по 24-сегментному полигону (`poly_area` в Прелюдии-R), НЕ π·r²: боковина dV 1509.43, front/back dV 3689.72 мм³.
- Фикс «врезной pushpull уничтожает Face → пол искать заново геометрически» УЖЕ встроен в `drill` Прелюдии-R (наследие ledger «Cam Task 3») — не удивляться отсутствию `face.valid?`-проверки.
- Фасады и slide_L/R не трогаются; slide-тела у выдвинутых ящиков лежат в группе со «странными» локальными координатами — это норма, не чинить.
- Пересборка убивает разметку «Присадка» на боковинах — RUN-блок пересоздаёт её в том же чанке (1 cline + 3 cpoint на боковину, ось Z 160,5, точки Y 902/614/262 закрыто / 1252/964/612 выдвинуто). Инвариант по модели: тег «Присадка» = 40 cline + 160 cpoint.
- Прочие инварианты: группы 233 (5 снесли + 5 построили на ящик), узловые bbox == preflight, слоёв 16 → 20 (T0).

Правила работы (обязательны, источник — ledger):
- Общение по-русски; пользователь бережёт токены — короткие отчёты, но ledger вести подробно (запись «Drawer Task N: …» после каждого таска).
- Perед работой с моделью — preflight по Task 1 плана (get_version, list_components recursive == 233/truncated=false, зонд 10 ящиков). Модель могла измениться с 2026-07-12 (пользователь работает в SketchUp сам) — при любом расхождении СТОП.
- Мутирующий eval_ruby — самодостаточный чанк: ровно одна операция start_operation → commit_operation, abort_operation в rescue, JSON-отчёт последним выражением (puts не виден). ok:false откатывается сам; MCP undo — ровно один, только для закоммиченного, но не прошедшего сверку чанка.
- Порог сверок bounds/bbox — 0,2 мм (1e-6″ ложно срабатывает); объёмы ±5 мм³.
- Solid-tools ЗАПРЕЩЕНЫ (убьют guide-геометрию); только add_circle+pushpull внутри definition.entities.
- `model.save` НЕ вызывать — сохраняет пользователь. Видимость тегов не трогать вне операций с возвратом.
- Git в фазе исполнения не меняется (только модель + ledger).

## PLAN QUALITY WARNING

План писался на большую задачу и может содержать ошибки: неточности координат, упущенные зависимости, расхождения с фактическим состоянием модели (пользователь работает в SketchUp сам — документы могли разойтись с живой моделью).

**Если что-то не сходится (preflight-зонд, отчёт чанка, поведение SketchUp):**
1. СТОП до проблемного шага
2. Ясно описать проблему
3. Объяснить, почему план не работает или выглядит неверным
4. Спросить пользователя, как поступить

НЕ обходить проблемы молча и НЕ отклоняться от плана без одобрения пользователя.
