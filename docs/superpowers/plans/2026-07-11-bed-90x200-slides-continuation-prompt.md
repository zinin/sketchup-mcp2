## TASK

Continue работу над «Кровать 90×200 с ящиками — модель в SketchUp через MCP». В сцене теперь ЧЕТЫРЕ кровати: старые монолитные `Bed_A`/`Bed_B` и новые МОДУЛЬНЫЕ `Bed_C` (2 ящика) / `Bed_D` (3 ящика), построенные из-за ограничения материала (щиты 2000×600, детали >2000 не выкраиваются). Следующая фаза: **направляющие (боковые шариковые)** — конкретный объём работ назовёт пользователь.

## CRITICAL: DO NOT START WORKING

**STOP. READ THIS CAREFULLY.**

After loading all context below, you MUST:
1. Read the documents and understand the context
2. Report what you understood (brief summary)
3. **WAIT for explicit user instructions** before taking ANY action

**DO NOT:**
- Start implementing tasks
- Make any code changes
- Run any commands (except reading documents)
- Assume what task to work on next

**The user will tell you exactly what to do.** Until then, only read and summarize.

## DOCUMENTS

- Design: `docs/superpowers/specs/2026-07-10-bed-90x200-design.md` — описывает только A/B и частично устарел (ламели 890, матрасы Y 19–919, планки-стяжки, модульные C/D в нём отсутствуют); истина = живая модель + ledger
- Plan: `docs/superpowers/plans/2026-07-10-bed-90x200-model.md` — тримлен (все таски → ✅); **Global Constraints и Прелюдия обязательны для любых правок модели**. Полный план: `git show 4b7be1b:docs/superpowers/plans/2026-07-10-bed-90x200-model.md`
- Progress ledger: `.superpowers/sdd/progress.md` (вне git) — ЧИТАТЬ ЦЕЛИКОМ; полная геометрия Bed_C/Bed_D есть ТОЛЬКО в записи «Постройка Bed_C/Bed_D» (⚠ она стоит в файле ВЫШЕ «Правки 4», хронологически было наоборот)
- Research: `docs/bed-drawer-slides-research.md` — §6 чек-лист (для скрытого монтажа, отменён), §5 опровергнутые утверждения (НЕ использовать), §8 непроверенное; сырьё части 2 (боковые шариковые на 750 мм, БЕЗ верификации): `.superpowers/sdd/2026-07-10-research-750mm-raw.md`

Read plan (Global Constraints + Прелюдия), ledger, research §5/§8 first.

## PROGRESS

**Completed (всё — в живой модели SketchUp; git-коммитов исполнение не создаёт):**
- [x] Tasks 1–12, Правки 1–3 — обе кровати A/B, теги, узел перегородок (см. ledger)
- [x] Проверка после перезагрузки: Правка 3 цела (101 группа, перегородки ровно 5 938 920 мм³)
- [x] Решение пользователя: ВСЕ направляющие — боковые шариковые (дешевле); скрытый монтаж отменён, допроверка 750-мм систем скрытого монтажа не нужна
- [x] Правка 4: планки-стяжки по полу в A и B против продольной шаткости (щит 18 плашмя 150 мм, Z 0–18; передние Y 788–938, средние Y 400–550; по камерам встык; 10 групп, тег Корпус)
- [x] Постройка Bed_C/Bed_D (модульная схема): Bed_C = 2 модуля по 1028 (камеры 992, ящики 967, фасады 1022), Bed_D = 3 модуля 686/684/686 (камеры 650/648/650, ящики 625/623/625, фасады 680); стыковые Γ-стенки с язычком (объём 5 981 040 мм³); без изголовий; все ящики на шариковых; 122 группы, все bbox 0.0; итого в модели 233 группы
- [x] Модель сохранена пользователем (Z:\git\sketchup\krovat90x200\1.skp)

**Remaining:**
- [ ] Фаза «направляющие» — ЖДАТЬ конкретики от пользователя. Кандидаты: (а) `A.drawer2` в Bed_A всё ещё «скрытый монтаж» (коробка 991, рельсы 750×40×15 ПОД ДНОМ Z 25–40) — перестроить под шариковые по образцу drawer1 (коробка 976, рельсы 12,5 на стенки Z 138–183), ЕСЛИ Bed_A остаётся актуальной; (б) детализация упрощённых тел направляющих; (в) точечная допроверка фактов: 12,5 vs 12,7 мм/сторону (в модели 12,5), нагрузка пары 45-мм шариковых на 750 мм
- [ ] Судьба Bed_A/Bed_B рядом с модульными C/D — решает пользователь; удалять что-либо ТОЛЬКО по явному подтверждению

## SESSION CONTEXT

- Сцена (мировые bbox): `Bed_A` [-18,0,0 → 2056,1306,900] (изголовье; drawer1 выдвинут, drawer2 «скрытый монтаж» закрыт), `Bed_B` [2656,0,0 → 4712,1306,580] (средний выдвинут), `Bed_C` [5312,0,0 → 7368,1306,580] (левый выдвинут), `Bed_D` [7968,0,0 → 10024,1306,580] (средний выдвинут). Зазоры 600.
- Модульная схема C/D: модуль = wall_back + наружная стенка 920×420 + стыковая(ые) Γ-стенка(и) + царга МЕЖДУ стенками + бруски cleat + планки stretcher (длина = камера). Стыковая Γ-стенка: поле 920×360 + язычок Y 920–938 до Z 420 (заполняет 130-мм полосу над фасадами; до матраса 1 мм); на стыке две стенки = 36 мм. Фанеры 18 в каркасе C/D нет; межсекционные стяжки — крепёж, вне скоупа модели.
- Конвенции ящиков (сверены пробой по Bed_A, повторены в C/D): выдвинутый ящик = коробка+фасад смещены на Y +350, РЕЛЬСЫ ОСТАЮТСЯ на стенках (Y 188–938, Z 138–183, толщина 12,5 — заполняют зазор целиком); дно закрытого ящика Y 194–930, низ дна Z 53; фасад закрытого Y 938–956, Z 16–286; узлы drawer в C/D с identity-трансформом, все детали построены в мировых координатах.
- Раскладка ламелей (эталон для новых построек): X-старты 43; 199,7; 356,3; 513; 669,7; 826,3; 983; 1139,7; 1296,3; 1453; 1609,7; 1766,3; 1923 (+сдвиг кровати: C +5312, D +7968), каждая 90×890×20, Y 24–914, Z 360–380.
- Трансформы: Bed_A несёт origin X −18 (узлы компенсируют); перед мутацией внутри узла — assert: bed.transformation × node.transformation == identity (±1e-6), иначе abort; bbox сверять в МИРОВЫХ координатах (8 углов definition.bounds × накопленный трансформ), голый Group#bounds для вложенных узлов ВРЁТ (даёт систему родителя).
- Теги у пользователя: Матрасы/Ламели/Ящики/Фасады ящиков ВЫКЛЮЧЕНЫ, Направляющие/Корпус включены. Для «полного» скриншота: временно включить (операция) → кадр → вернуть в точности (операция) — так делалось в этой сессии. Камера оставлена на C+D; рецепт фасадной камеры: target = центр bbox, dir (1, 1.1, 0.75) нормализованный, eye = target + dir×diag×1.25, up (0,0,1) — камера не мутация, операция не нужна.
- Технические правила правок (Global Constraints плана, обязательны): каждая правка = самодостаточный eval_ruby-чанк с прелюдией целиком (`TOPLEVEL_BINDING.dup` — локальные переменные между вызовами не живут); чанк сам оборачивает мутации `model.start_operation` → `commit_operation`, `abort_operation` в rescue; сверка bbox ±0,2 мм; при ошибке ровно один вызов MCP-инструмента `undo` → исправить скрипт → повторить; `model.save` НЕ вызывать (пользователь сохраняет сам); чужие сущности не трогать; работа inline, субагентов не разворачивать.
- Незакрытое по шариковым направляющим: вердикт 12,5 vs 12,7 мм/сторону НЕ дан (в модели 12,5 — для модели ок, финал по даташиту купленных); нагрузка пары 45 мм на длине 750 НЕ верифицирована; при желании — точечная ручная проверка по официальным каталогам (дёшево), сырьё в `...-750mm-raw.md` использовать только со сверкой по первоисточникам. Тяжёлые мульти-агентные раны НЕ запускать без явного запроса — пользователь бережёт токены.
- Материал пользователя: мебельные щиты 2000×600×18; есть фанера ФСФ 18, но использовать её он НЕ хочет (потому и модульная схема). Ящики по спеке — фанера 12/6.
- Git: ветка `feature/bed-90x200`; `docs/bed-drawer-slides-research.md` untracked — НЕ коммитить без просьбы; план тримлен и закоммичен; перед будущим PR все файлы `docs/superpowers/` удаляются из ветки (правило CLAUDE.md пользователя).
- Общаться с пользователем по-русски.

## PLAN QUALITY WARNING

Спека и план писались до модульной схемы и могут расходиться с реальной моделью; по C/D они не обновлялись сознательно — истина = живая модель + ledger.

**If you notice any issues during implementation:**
1. STOP before proceeding with the problematic step
2. Clearly describe the problem you found
3. Explain why the plan doesn't work or seems incorrect
4. Ask the user how to proceed

Do NOT silently work around plan issues or make significant deviations without user approval.

## INSTRUCTIONS

1. Read the documents listed above
2. Understand current progress and session context
3. Provide a brief summary of what you understood
4. **STOP and WAIT** — do NOT proceed with any implementation
5. Ask: «SketchUp запущен и MCP подключён? Что делаем по направляющим — и остаются ли Bed_A/Bed_B в сцене?»
