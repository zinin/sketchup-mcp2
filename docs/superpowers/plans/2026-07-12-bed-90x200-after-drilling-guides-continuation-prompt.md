## TASK

Продолжение работ над SketchUp-моделью кроватей 90×200 (репозиторий sketchup-mcp2, ветка `feature/bed-90x200`).

Фаза «Присадка направляющих SETE SB-45750» **завершена целиком** (все 7 тасков, приёмка пройдена, модель сохранена пользователем 2026-07-12). Следующую задачу назовёт пользователь.

## CRITICAL: DO NOT START WORKING

**STOP. READ THIS CAREFULLY.**

After loading all context below, you MUST:
1. Read the documents and understand the context
2. Report what you understood (brief summary)
3. **WAIT for explicit user instructions** before taking ANY action

**DO NOT:**
- Start implementing tasks
- Make any code changes or model mutations
- Run any commands (except reading documents)
- Assume what task to work on next

**The user will tell you exactly what to do.** Until then, only read and summarize.

## DOCUMENTS

- Progress ledger (вне git, ЧИТАТЬ ЦЕЛИКОМ — единственная полная летопись модели): `.superpowers/sdd/progress.md`
- Design присадки (исполнено): `docs/superpowers/specs/2026-07-11-drawer-slides-drilling-guides-design.md`
- Plan присадки (тримлен, все таски ✅, коммит `8be59a8`): `docs/superpowers/plans/2026-07-11-drawer-slides-drilling-guides.md`; полный текст при надобности: `git show 7791ff7:docs/superpowers/plans/2026-07-11-drawer-slides-drilling-guides.md`
- Рецепты SketchUp Ruby API (по мере надобности): `docs/sketchup-ruby-cookbook.md`

## PROGRESS

Фаза присадки — все таски ✅ (подробности — ledger, записи «Slides Task 1..7»):
- [x] Task 1: preflight (233 группы, все bbox носителей 0.0, eval-гейт ok)
- [x] Task 2: `A.drawer2` перестроен под боковые шариковые (коробка 976, X 1049,5–2025,5; рельсы на стенках камеры Z 138–183; фасад не тронут; bbox узла не изменился)
- [x] Tasks 3–6: присадка Bed_A/B/C/D — **40 guide-линий + 160 guide-точек** на теге «Присадка», все мировые координаты 0.0
- [x] Task 7: кадры приёмки, чек-лист пройден, видимость возвращена («Присадка» осталась ON)

**Remaining: НЕТ — план закрыт.** Новая фаза = новый цикл (brainstorming → writing-plans) по слову пользователя.

## SESSION CONTEXT

Состояние модели (сохранена пользователем 2026-07-12, `Z:\git\sketchup\krovat90x200\1.skp`; сохраняет ТОЛЬКО пользователь — `model.save` не вызывать):
- **233 группы**; 8 тегов: Layer0/Направляющие/Корпус/Присадка = on, Матрасы/Ламели/Ящики/Фасады ящиков = off; HideConstructionGeometry = false; камера оставлена на Bed_A.
- Кровати: `Bed_A` [-18,0,0 → 2056,1306,900] (изголовье, 2 ящика), `Bed_B` (3 ящика), `Bed_C` (модульная, 2), `Bed_D` (модульная, 3). Выдвинутые ящики (+350 по Y): `A.drawer1`, `B.drawer2`, `C.drawer1`, `D.drawer2`.
- Присадка: ось Z 160,5 (середина рельса 138–183); корпусные плоскости — линия Y 188–938, точки Y 901/837/677/549/261; ящичные закрытые — 902/614/262; выдвинутые — линия 538–1288, точки 1252/964/612. Источник точек — схема GTV PK-0H45…GX_250_750 (SETE своей не публикует); перед реальным сверлением пользователь сверяет разметку с купленными направляющими (дисклеймер спеки §4.1, повторно не обсуждать).
- При первом подключении MCP: `get_version`, затем `list_components` (recursive) — убедиться, что групп 233 и тег «Присадка» на месте; таски, отмеченные в ledger complete, НЕ перестраивать.

Технические грабли из прошлых сессий (ОБЯЗАТЕЛЬНЫ к учёту в будущих eval_ruby-чанках):
- `Bed_A`/`Bed_B` несут ненулевые трансформы (у Bed_A origin X −18, компенсация +18 в узлах): мировые bbox = definition.bounds × накопленный трансформ; голый `Group#bounds` узла живёт в системе родителя (+18 по X у Bed_A). Перед мутацией — assert «накопленный трансформ = identity/чистая трансляция».
- Самопроверка «bounds детали не изменились» с порогом 1e-6″ ложно срабатывает на разовый шум ленивого пересчёта bbox SketchUp сразу после большой операции; рабочий порог — **0,2 мм** (= плановый допуск, одобрен пользователем).
- Упавший (незакоммиченный) чанк с `abort_operation` в rescue откатывается сам — MCP `undo` нужен только после закоммиченного, но не прошедшего сверку чанка.
- Диагностика без следов в модели: зонд `start_operation` + `ensure abort_operation`.
- `eval_ruby` возвращает `.to_s` последнего выражения; `puts` не виден — завершать чанк JSON-строкой.
- Формат чанка: полная самодостаточная прелюдия, ровно одна операция `start_operation`→`commit_operation` (abort в rescue), JSON-отчёт, сверка ±0,2 мм, инвариант 233 группы, чужие сущности не трогать.

Правила общения/процесса: по-русски; пользователь бережёт токены (тяжёлые мульти-агентные раны не запускать); режим модельных правок — **inline** (superpowers:executing-plans), каждый `eval_ruby` проходит per-call review, чанки не объединять; ledger дописывать после каждого таска; перед будущим PR удалить `docs/superpowers/` из ветки (правило CLAUDE.md пользователя).

Кандидаты на следующую фазу (из ledger; РЕШАЕТ ПОЛЬЗОВАТЕЛЬ — не начинать самостоятельно):
- детализация упрощённых тел направляющих;
- точечная допроверка «12,5 vs 12,7 мм» и нагрузки пары шариковых 45 мм на 750 мм (сырьё без верификации: `.superpowers/sdd/2026-07-10-research-750mm-raw.md`);
- пересчёт закупки §9 под модульную схему Bed_C/Bed_D (не делался);
- судьба Bed_A/Bed_B рядом с модульными C/D (удаление — только по явному подтверждению);
- финализация ветки `feature/bed-90x200` (когда пользователь решит).

## PLAN QUALITY WARNING

Для новой фазы плана ещё нет. Если пользователь попросит модельные правки без плана — предложить штатный цикл superpowers (brainstorming → writing-plans) либо выполнить точечную правку по правилам чанков выше. При ЛЮБОМ расхождении живой модели с ledger — СТОП и вопрос пользователю.

## INSTRUCTIONS

1. Прочитай документы выше (ledger — целиком)
2. Пойми текущее состояние модели и контекст
3. Дай краткое резюме понятого
4. **STOP and WAIT** — никаких действий
5. Спроси: «Что делаем дальше?»
