# Continuation prompt — v0.3.0 release ceremony (sketchup-mcp2), 2026-07-04

## TASK

Continue the **v0.3.0 release** of sketchup-mcp2. The deep-review work (batches 1+2, all 17 plan tasks + final mesh-review) is DONE and already on branch `fix/deep-review-p2`. This session's job is the **release ceremony**: merge **PR #3** into `master`, then run `docs/release.md` §3–7 (build → TestPyPI → PyPI → sign → tag → GitHub Release; Extension Warehouse optional). The version bump to 0.3.0 and a full **live verification of the fresh 0.3.0 `.rbz`** are already complete (see PROGRESS).

## CRITICAL: DO NOT START WORKING

**STOP. READ THIS CAREFULLY.**

After loading all context below, you MUST:
1. Read the documents and understand the context
2. Report what you understood (brief summary)
3. **WAIT for explicit user instructions** before taking ANY action

**DO NOT:**
- Merge the PR, push, publish to PyPI/TestPyPI, tag, create a GitHub Release, or run any release step
- Make code changes or run any commands (except reading documents and cheap read-only `git`/`gh` status/log)
- Assume which step to run next

**Several remaining steps are irreversible or owner-gated** (PyPI is IMMUTABLE; Trimble signing and PyPI creds are the owner's; tagging/publishing is outward-facing). **The user will tell you exactly what to do.** Until then, only read and summarize.

## DOCUMENTS

- **Release runbook:** `docs/release.md` — the authoritative step-by-step (§0 pre-flight → §1 bump [DONE] → §2 tests → §3 build → §4 TestPyPI → §5 PyPI → §6 tag+Release → §7 EW). Read §3–7 carefully; that is the remaining work.
- **Progress ledger:** `.superpowers/sdd/progress.md` — read the tail block **«=== RELEASE v0.3.0 START (2026-07-04) ===»** in full; it is the durable record of the bump, PR #3, the fresh-`.rbz` build, and the live verification. Also skim the two earlier sections (batch-1 and batch-2) for the owner-reminder history that the PR body already carries.
- The 17-task plan/design docs are **complete and were `git rm`'d** from tracking (commit `13763da`); they live only in git history (reachable via `e28653c` or earlier). You do **not** need them for the release.

## PROGRESS

**Done (this branch / prior sessions):**
- [x] All 17 deep-review tasks (batches 1+2), final whole-branch mesh-review (0 confirmed Critical), AUTO×20 `16f3499` + disputed fix `6e5f631`.
- [x] `git rm -r docs/superpowers/` (7 tracked files) → commit **`13763da`**. Owner's untracked prompt archive (incl. this file) stays on disk; absent from PR diff.
- [x] **Version bump 0.2.0 → 0.3.0** → commit **`8b55204`**: all 7 version files + BOTH MIN floors (`MIN_RUBY`, `MIN_PYTHON`) → 0.3.0 (exact-match handshake; mandatory contract-break bump). `uv.lock` refreshed; `docs/release.md` contract-break note updated «pending»→«shipped in v0.3.0». Suites green: **Ruby 423/1140/0/0, Python 177**.
- [x] **PR #3 OPEN** — https://github.com/zinin/sketchup-mcp2/pull/3 (`fix/deep-review-p2` → `master`, 65 commits). Body carries the full owner-reminder checklist. `docs/superpowers/` absent from the 62-file diff. Branch: 65 ahead / 0 behind `origin/master`.
- [x] **Fresh v0.3.0 `.rbz` built + verified** (both variants, 28 files, eval gate warehouse=false/github=true, ext.version 0.3.0): `mcp_for_sketchup/mcp_for_sketchup_v0.3.0-{warehouse,github}.rbz`.
- [x] **LIVE verification of the fresh 0.3.0 artifact** (SketchUp 2026 build 26.1.256, eval on): handshake 0.3.0↔0.3.0 `compatible=true`; all post-mesh-review validations confirmed (per-type min-dims, polar-chord 0.0381<0.04, non-zero scale, max_depth 1..10, dovetail ≤60, chamfer/fillet stats); batch-2 features (pagination, concise, case-insensitive find, bbox null/non-null, name, default 100³, export warning, screenshot metadata); absolute-position idempotency; full health sweep all categories + `eval_ruby`. **Release artifact is sound.**

**Remaining (release ceremony — the order):**
- [ ] **Merge PR #3 into `master`.** ⚠ **Use a MERGE-COMMIT** (`gh pr merge --merge`), **NOT squash/rebase** — the PR body AND the ledger cite commit SHAs for revert instructions (`git revert 5de1987`, `889293e`, `5e545f4`, …); squash/rebase would erase those SHAs from `master` and break the instructions.
- [ ] **`docs/release.md` §2–3:** re-run suites from the merged `master` tip (build-runner), then build artifacts: `uv build` + `uvx twine check dist/*`, and rebuild both `.rbz` from the merged tip (`ruby package.rb --variant=warehouse` / `--variant=github`).
- [ ] **§4 TestPyPI rehearsal** → verify install in a throwaway venv (needs `~/.pypirc`; owner-gated).
- [ ] **§5 PyPI upload** — ⚠ IMMUTABLE; owner-gated; confirm before running.
- [ ] **§6 Git tag `v0.3.0` + GitHub Release** — attach both `.rbz` + wheel + sdist. The **github `.rbz` must be Trimble-self-signed first** (owner, manual). Draft release notes (call out the absolute-`transform_component.position` semantics + the exact-match 0.3.0 handshake break).
- [ ] **§7 Extension Warehouse** (optional, 2–3 day review): resolve product decision **T-47** (physically exclude `eval.rb` from the warehouse build) before submitting the warehouse `.rbz` unsigned.

## SESSION CONTEXT (implicit knowledge — not fully in the docs)

- **Merge method is the one real trap.** Merge-commit only (see above). No squash, no rebase.
- **What the assistant can do vs owner-gated.** Assistant (via build-runner): §2 tests, §3 `uv build`/`twine check`/both `.rbz`, draft release notes, verify artifact contents. Owner only: TestPyPI/PyPI upload (creds in `~/.pypirc`), Trimble self-signing of the github `.rbz`, the actual tag push + Release publish decision, EW web form. **Build/test/package ALWAYS via the `build-runner` subagent — never directly in the main session.**
- **Build artifacts from the MERGED master tip**, not from the branch, so the tag matches what ships. After merge: `git fetch`, check out / fast-forward `master`, build there.
- **gh works** (owner: `zinin`, ssh, scope `repo`). `git@github.com:zinin/sketchup-mcp2.git`. v0.2.0 is the current latest release; tags v0.0.1..v0.2.0 exist.
- **Versions are now 0.3.0 everywhere** (7 files + both MIN floors). Do NOT re-bump. The three compat invariant tests (`test_min_le_max_invariant`, `test_max_ruby_matches_python_version`, `test_max_python_matches_server_version`) are green at 0.3.0.
- **The live Python MCP client is an editable install** (`uv run --directory … python -m sketchup_mcp`, env `/home/zinin/.venvs/sketchup-mcp2`); its `installed metadata` may still read 0.2.0 but runtime `CLIENT_VERSION` = source `__version__` = 0.3.0. A `uv sync` on next run refreshes the metadata.
- **Owner manual cleanup still pending:** the live test bound a throwaway model to a temp `.skp` (close without saving); `rm -rf .venv.broken-task8/` after the MCP-server restart (already restarted this session).
- **Do NOT touch untracked:** `.venv.broken-task8/`, `.gemini/`, `.superpowers/`, `docs/superpowers/plans/*prompt*.md` (owner archive — includes this file). `.rbz` are gitignored build outputs.

## Critical invariants (violating = stop)

- Commits are **English, conventional, NO AI attribution**. `A.subtract(B) == B − A` — never «fix». Literal source-guard tests — no formatters. Wire protocol / handshake untouched.
- **PyPI is immutable**: once `0.3.0` is uploaded it can never be re-uploaded, even after deletion — a mistake means bumping to `0.3.1`. Rehearse on TestPyPI first.

## RELEASE-RUNBOOK QUALITY WARNING

`docs/release.md` is a good runbook but was written across earlier releases and may have small gaps or drift. If a step doesn't match reality (unexpected `gh`/`twine`/`uv` behavior, a signing prompt, a divergence, an artifact-count mismatch, a question about release-notes content) → **STOP and ask the user.** Do not silently work around it, and never run an irreversible/outward-facing step (PyPI upload, tag push, Release publish) without explicit go-ahead.

## INSTRUCTIONS

1. Read `docs/release.md` (§3–7) and the ledger tail block
2. Understand current progress and session context
3. Provide a brief summary of what you understood
4. **STOP and WAIT** — do NOT run any release step
5. Ask: "What would you like me to work on?"
