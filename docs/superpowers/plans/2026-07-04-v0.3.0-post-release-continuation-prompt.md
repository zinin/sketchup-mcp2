# Continuation prompt — sketchup-mcp2 v0.3.0 post-release (2026-07-04)

## TASK

The **v0.3.0 release core is DONE and published** — `docs/release.md` §1–§6 are complete
(merge → tests → build → TestPyPI → PyPI → git tag + GitHub Release). This session handles
only the **remaining, optional, owner-gated tail**: the **Extension Warehouse submission (§7)**
— which is blocked on **product decision T-47** — plus **owner cleanup**. There is no code
plan left to execute.

## CRITICAL: DO NOT START WORKING

**STOP. READ THIS CAREFULLY.**

After loading all context below, you MUST:
1. Read the documents and understand the context
2. Report what you understood (brief summary)
3. **WAIT for explicit user instructions** before taking ANY action

**DO NOT:**
- Submit anything to Extension Warehouse, sign/build/upload any artifact, delete branches, or run any release step
- Make code changes or run any commands (except reading documents and cheap read-only `git`/`gh` status/log)
- Assume T-47 is decided, or assume EW submission is wanted at all (it's optional)

**§7 is outward-facing and owner-gated, and T-47 is an unresolved product/security decision.**
The user will tell you exactly what to do. Until then, only read and summarize.

## DOCUMENTS

- **Release runbook:** `docs/release.md` — read **§7 (Extension Warehouse submission)** and the
  **"Warehouse vs GitHub release"** section in full. §7 carries the stable EW form values,
  the Description / Testing-Instructions / Release-Notes templates (paste-verbatim), the
  screenshot guidance, and the `.rbz` well-formedness self-check. §0–§6 are DONE — skim only.
- **Progress ledger:** `.superpowers/sdd/progress.md` — read the tail block
  **"=== RELEASE v0.3.0 START (2026-07-04) ==="** in full, especially the
  **"RELEASE v0.3.0 EXECUTED"** and **"REMAINING (owner…)"** lines — the durable record of
  the whole ceremony. (This is UNTRACKED scratch; the only durable record since the plan/design
  docs were git-rm'd in `13763da`.)
- The 17-task plan/design docs are **off-disk** (removed in `13763da`); recover from git history
  (reachable via `e28653c` or earlier) only if genuinely needed — you will not need them here.

## PROGRESS

**Completed (prior sessions + the release session):**
- [x] Deep-review batches 1+2 — all 27 tasks (batch-1 ×10 + batch-2 ×17), two whole-branch
      mesh-reviews (0 confirmed Critical), PR #3.
- [x] Post-PR Codex P2 fix `83996ef` — flood-starved readable clients spared from the
      pre-handshake sweep (sweep moved after drain + saturation-gated `IO#wait_readable(0)` probe).
      Suites: Ruby 425/1149/0/0, Python 177.
- [x] Version bump 0.2.0 → 0.3.0 (`8b55204`): 7 version files + BOTH MIN floors → exact-match handshake.
- [x] **PR #3 merged as MERGE-COMMIT `13172a3`** (2 parents; all revert-cited SHAs verified ancestors of master).
- [x] §2 tests, §3 build (wheel+sdist+both `.rbz`, twine check PASSED) — all from the merged tip `13172a3`.
- [x] §4 TestPyPI rehearsal + fresh-venv install verify.
- [x] **§5 PyPI — LIVE** (IMMUTABLE): https://pypi.org/project/sketchup-mcp2/0.3.0/
- [x] Owner Trimble-signed the github `.rbz` → `mcp_for_sketchup_v0.3.0-github-signed.rbz` (205 683 b).
- [x] **§6 tag `v0.3.0` + GitHub Release PUBLISHED** (4 assets, not draft):
      https://github.com/zinin/sketchup-mcp2/releases/tag/v0.3.0

**Remaining (optional / owner-gated — the only open work):**
- [ ] **T-47 product decision (BLOCKS §7):** physically exclude `handlers/eval.rb` from the
      **warehouse** build (not just gate it off). Today the warehouse `.rbz` still SHIPS `eval.rb`
      but with `EVAL_ENABLED_BY_DEFAULT=false` (off + blocking security confirm). T-47 asks whether
      to go further and physically remove the file so the warehouse variant literally cannot eval
      — relevant because EW moderators may flag arbitrary-code-execution. Must be resolved BEFORE submitting.
- [ ] **§7 Extension Warehouse submission** (optional, ~2–3 business-day review): submit the
      **UNSIGNED** `mcp_for_sketchup_v0.3.0-warehouse.rbz` via the EW intake form
      (Encryption Type: **Encrypt** → Trimble signs after review). Web form is owner-only; assistant
      can prep artifact + paste-templates + verify `.rbz` well-formedness. If T-47 changes the build,
      the warehouse `.rbz` must be rebuilt first.
- [ ] **Owner cleanup:** `rm -rf .venv.broken-task8/`; close the throwaway `.skp` bound during live
      testing WITHOUT saving; optionally delete merged branch `origin/fix/deep-review-p2`
      (SHAs preserved in master via the merge-commit — safe to delete).

## SESSION CONTEXT (implicit knowledge — not fully in the docs)

- **T-47 is a genuine fork, not a rubber-stamp.** Physically excluding `eval.rb` means the warehouse
  variant loses `eval_ruby` entirely (the dispatch must also stop routing to it, or return a clean
  "not available in this build" error). Weigh: EW-review friction vs. feature parity. This is the
  owner's call; do not pick a side unprompted.
- **Assistant vs owner-gated.** Assistant (via `build-runner`): rebuild the warehouse `.rbz`,
  run the §7 zip self-check (expect total=28 / .rb=27 / root=`['mcp_for_sketchup.rb']`), assemble
  the paste-verbatim form text, draft EW release notes. Owner only: the EW web intake form, any
  signing, branch deletion, the `.skp`/venv cleanup.
- **Build/test/package ALWAYS via the `build-runner` subagent — never directly in the main session.**
- **Artifacts are gitignored build outputs.** Current on-disk `.rbz` were built from `13172a3`
  (`mcp_for_sketchup/mcp_for_sketchup_v0.3.0-{warehouse,github,github-signed}.rbz`). If T-47 changes
  the build, rebuild via `(cd mcp_for_sketchup && ruby package.rb --variant=warehouse)`.
- **Do NOT touch untracked:** `.venv.broken-task8/`, `.gemini/`, `.superpowers/`, `docs/superpowers/`
  (owner archive + this file). Do NOT commit anything under `docs/superpowers/` to master.
- **Repo state:** master tip `13172a3` == `origin/master`; tree clean apart from the untracked above.
  `gh` works (owner `zinin`, ssh, scope `repo`). Versions are 0.3.0 everywhere — do NOT re-bump.
- **The EW form values in §7 are stable across releases** — only Version Number (`0.3.0`),
  Release Notes, and occasionally the Description change. `product_id` is `MCP_FOR_SKETCHUP`.
  EW requires ≥1 screenshot; §7 lists three easy in-SketchUp captures.

## RELEASE-RUNBOOK QUALITY WARNING

`docs/release.md` §7 is paste-verbatim material that EW moderators execute literally. If a template
line doesn't match reality (menu labels, form field, compatibility list, screenshot step, a signing
prompt) → **STOP and ask the user.** Do not silently work around it, and never run an
outward-facing step (EW submit, branch deletion, any signing) without explicit go-ahead.

## INSTRUCTIONS

1. Read `docs/release.md` §7 + "Warehouse vs GitHub release", and the ledger tail block
2. Understand current progress and session context
3. Provide a brief summary of what you understood
4. **STOP and WAIT** — do NOT run any step
5. Ask: "What would you like me to work on?"
