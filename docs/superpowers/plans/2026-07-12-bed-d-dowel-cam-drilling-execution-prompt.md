## TASK

Исполнить implementation-план «Присадка Bed_D под шканты 8 мм и минификсы» (SketchUp-модель кроватей 90×200, репозиторий sketchup-mcp2, ветка `feature/bed-90x200`).

Режим исполнения: **inline** — скилл `superpowers:executing-plans` (выбор пользователя, зафиксирован в ledger; субагентов и мульти-агентные раны НЕ запускать — пользователь бережёт токены). Каждый `eval_ruby`-чанк проходит per-call review пользователя; чанки не объединять и не дробить.

## DOCUMENTS

- Design: `docs/superpowers/specs/2026-07-12-bed-d-dowel-cam-drilling-design.md`
- Plan: `docs/superpowers/plans/2026-07-12-bed-d-dowel-cam-drilling.md`
- Progress ledger (вне git, ЧИТАТЬ ЦЕЛИКОМ — летопись модели): `.superpowers/sdd/progress.md`
- Рецепты SketchUp Ruby API (по надобности): `docs/sketchup-ruby-cookbook.md`

Сначала прочитай все документы (ledger — целиком).

## IMPORTANT: DO NOT START WORK YET

После чтения документов:
1. Подтверди, что контекст загружен
2. Кратко изложи понимание (что делаем, сколько чанков, ключевые инварианты)
3. **ЖДИ явной команды пользователя** — никаких действий до неё

НЕ начинай исполнение, пока пользователь явно не скажет стартовать.

## SESSION CONTEXT

Решения brainstorming-сессии 2026-07-12 (сверх документов):

- **Объект — Bed_D** (была выделена пользователем в SketchUp). Bed_A/B/C не трогать.
- Пользователь выбрал **высверлить реальные отверстия** под шканты и стяжки (мой вариант «только точки» отклонён) + guide-точки в устьях; шурупные места (бруски J5, ламели J6) — только точки, без вырезов.
- Крепёж: минификс 15 мм со штоком **B34** — «типовой, ещё не куплен»: перед реальным сверлением пользователь сверяет B-размер с купленной фурнитурой (дисклеймер в спеке §2.2; повторно не обсуждать). Шканты 8×30 (торцевые) и 8×25 (межсекционные пласть-к-пласти).
- Скоуп-решение пользователя: корпус модулей + межсекционные стыки + шурупные точки брусков/ламелей; **ящики и фасады — вне** (фанера 12 мм не несёт минификс 15×13, шкант 8 оставляет 2 мм стенки).
- Теги: папка «Присадка D», 8 тегов «сверло×глубина» (выбор пользователя, подтверждён дважды). Существующие 8 тегов модели, включая «Присадку» направляющих, не трогать.
- **Отклонено:** solid-tools для вырезов (пересоздают группу → погибнут guide-точки присадки направляющих, живущие внутри тех же стенок; резать ТОЛЬКО add_circle+pushpull внутри definition.entities); guide-«кресты» Ø15; один общий тег.
- Канал под шток: в модели **26 мм** (перемычка 0,5 мм до стакана — касание граней даёт мусорную геометрию), реальные 36 мм — в имени тега. Глубже стакана канал физически не идёт.
- Матрица коллизий рассчитана при написании плана; в ней уже учтены два неочевидных решения: J5-шуруп mod2 центровой смещён на **X 9046** (на центре камеры 8996 — лобовое пересечение с пилотом ламели slat_07), язычковый шкант J4 на **Z 150** (выше 290 занято обратной стороной J2). Любое изменение координат → пересчитать матрицу вручную, иначе менять запрещено.
- Зонд Bed_D снят 2026-07-12 в планирующей сессии: все bbox/объёмы/трансформы/guides — отклонение 0.0 от таблиц плана; модель в этой сессии НЕ мутировалась. Task 1 (preflight) всё равно сверяет заново — при любом расхождении СТОП и вопрос пользователю.

Технические грабли (ОБЯЗАТЕЛЬНЫ, из ledger):
- `eval_ruby` возвращает `.to_s` последнего выражения; `puts` не виден — чанк завершается JSON-строкой.
- Ровно одна операция `start_operation`→`commit_operation` на чанк, `abort_operation` в rescue. Упавший незакоммиченный чанк откатывается сам; MCP `undo` — только для закоммиченного, но не прошедшего сверку.
- Порог самопроверок bounds — **0,2 мм** (1e-6″ ложно срабатывает на шум ленивого пересчёта bbox).
- Bed_D несёт трансляцию +7968 по X; assert «накопленный трансформ = чистая трансляция» перед мутацией каждой детали (в коде плана есть).
- Инвариант после каждого чанка: **233 группы** (вырезы и точки — не группы).
- `model.save` НЕ вызывать — сохраняет только пользователь.
- Ledger дописывать после КАЖДОГО таска (записи «Cam Task N»), включая фактические цифры отчётов.
- Объёмы Ruby `Group#volume` — кубические дюймы, переводить ×25.4³ (в коде плана учтено).
- `Layers#add_folder` / `LayerFolder#add_layer` — SketchUp 2021+; у пользователя SU 2026.

Правила общения: по-русски; пользователь бережёт токены — без лишних развёрнутых отчётов, но ledger вести подробно.

## PLAN QUALITY WARNING

План писался на большую задачу и может содержать ошибки: неточности координат, упущенные зависимости, неверные предположения об API. Если на любом шаге видишь проблему (отчёт чанка расходится с Expected, исключение внутри чанка, неожиданная геометрия на скриншоте):
1. СТОП перед проблемным шагом
2. Опиши проблему явно
3. Объясни, почему план тут не работает
4. Спроси пользователя, как действовать

НЕ обходи проблемы плана молча и НЕ отклоняйся от него без одобрения пользователя. Единственный штатный recovery: `ok:false` → чанк уже откатился сам → исправить → повторить; `ok:true` с расхождением Expected → ровно один MCP `undo` → исправить → повторить.
