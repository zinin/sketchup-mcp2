# Кровать 90×200 с ящиками — план моделирования в SketchUp

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Построить в живой модели SketchUp две кровати под матрас 900×2000×200 (A — 2 ящика + изголовье, B — 3 ящика) по спеке `docs/superpowers/specs/2026-07-10-bed-90x200-design.md`, каждая деталь — именованная группа с проверенным bbox.

**Architecture:** Последовательность самодостаточных `eval_ruby`-чанков: один узел кровати = один вызов = одна undo-операция. Каждый чанк возвращает JSON-отчёт bbox (мм) построенных групп; исполнитель сверяет его с эталонной таблицей Expected прямо в таске. В конце узлы оборачиваются в группы `Bed_A`/`Bed_B`, снимается изометрический скриншот.

**Tech Stack:** SketchUp MCP tools (`get_version`, `eval_ruby`, `undo`, `list_components`, `get_viewport_screenshot`), SketchUp Ruby API, паттерны `docs/sketchup-ruby-cookbook.md` (`make_box`, материалы, операции).

## Global Constraints

- Все размеры — **мм**; в Ruby API только через деление на 25.4 (`make_box` конвертирует сам; вручную не конвертировать).
- Каждый чанк **обязан** сам оборачивать правки: `model.start_operation(...)` → `commit_operation`, с `abort_operation` в rescue. `eval_ruby` этого НЕ делает. Операции не вкладывать.
- Чанки самодостаточны: `eval_ruby` исполняется в `TOPLEVEL_BINDING.dup`, локальные переменные и лямбды между вызовами НЕ сохраняются — прелюдию повторять в каждом чанке целиком.
- Имена групп — строго из этого плана (совпадают со спекой §5–6). Материалы: `MCP Pine`, `MCP Plywood`, `MCP Metal`, `MCP Mattress`.
- Существующие сущности пользователя не трогать; `model.save` не вызывать; удалять что-либо — только по явному подтверждению пользователя.
- Верификация: возвращённый JSON сравнивать с таблицей Expected таска; расхождение любой координаты > 0.2 мм = провал шага.
- Recovery при провале: один вызов MCP-инструмента `undo` (откатывает весь чанк — одну операцию), исправить скрипт, повторить чанк. Не «допиливать» модель дополнительными правками.
- Один чанк = один вызов `eval_ruby`. Скрипты подавать без сокращений, включая прелюдию.

## Прелюдия (повторяется в начале КАЖДОГО строительного чанка)

Ниже в тасках она обозначена комментарием `# --- PRELUDE --- (вставить целиком блок «Прелюдия» из docs/superpowers/plans/2026-07-10-bed-90x200-model.md)`.
При исполнении заменять этот комментарий полным текстом:

```ruby
require "json"
mm = 25.4
model = Sketchup.active_model

def make_box(parent_ents, x_mm, y_mm, z_mm, w_mm, d_mm, h_mm)
  mm = 25.4
  grp = parent_ents.add_group
  face = grp.entities.add_face(
    [x_mm/mm,          y_mm/mm,          z_mm/mm],
    [(x_mm + w_mm)/mm, y_mm/mm,          z_mm/mm],
    [(x_mm + w_mm)/mm, (y_mm + d_mm)/mm, z_mm/mm],
    [x_mm/mm,          (y_mm + d_mm)/mm, z_mm/mm]
  )
  sign = face.normal.z >= 0 ? 1 : -1
  face.pushpull(sign * h_mm / mm)
  grp
end

matl = lambda do |name, r, g, b|
  m = model.materials[name] || model.materials.add(name)
  m.color = Sketchup::Color.new(r, g, b)
  m
end
pine  = matl.call("MCP Pine",     230, 199, 154)
ply   = matl.call("MCP Plywood", 240, 221, 176)
metal = matl.call("MCP Metal",   158, 164, 171)
mgray = matl.call("MCP Mattress", 217, 217, 222)

part = lambda do |ents, name, material, x, y, z, w, d, h|
  g = make_box(ents, x, y, z, w, d, h)
  g.name = name
  g.material = material
  g
end

gbox = lambda do |g|
  b = g.bounds
  [b.min.x, b.min.y, b.min.z, b.max.x, b.max.y, b.max.z].map { |v| (v * mm).round(1) }
end

report = lambda do |node|
  h = {}
  node.entities.grep(Sketchup::Group).each { |g| h[g.name] = gbox.call(g) }
  h[node.name] = gbox.call(node)
  h
end
```

Пояснения: `make_box(ents, x, y, z, w, d, h)` — куб от угла (x,y,z) размером w×d×h мм (рецепт из cookbook, защищён от инверсии нормали); `def` персистентен между вызовами, но повторное определение безвредно. `matl` идемпотентен (`materials.add` с существующим именем не вызывается повторно).

---

### Task 1: Preflight — соединение, gate, чистота имён

**Files:**
- Живая модель SketchUp через MCP; репозиторий не меняется.
- Reference: спека §8; `CLAUDE.md` (eval gate, версии).

**Interfaces:**
- Produces: подтверждение — SketchUp доступен, `eval_ruby` включён, конфликтов имён нет; строка версии SketchUp (нужна Task 12: скриншот требует SU 2026+).

- [ ] **Step 1: Загрузить схемы MCP-инструментов**

Вызвать `ToolSearch` с query:
`select:mcp__sketchup__get_version,mcp__sketchup__eval_ruby,mcp__sketchup__undo,mcp__sketchup__list_components,mcp__sketchup__get_viewport_screenshot,mcp__sketchup__export_scene`

Expected: схемы шести инструментов загружены.

- [ ] **Step 2: Проверить связку версий**

Вызвать `mcp__sketchup__get_version` без аргументов.
Expected: JSON с verdict-совместимостью (`compatible: true` / отсутствие ошибки). Если соединения нет — попросить пользователя запустить SketchUp и стартовать сервер (`Plugins → MCP Server → Start`), повторить.

- [ ] **Step 3: Probe eval_ruby + проверка конфликтов имён**

Вызвать `mcp__sketchup__eval_ruby` с кодом:

```ruby
require "json"
model = Sketchup.active_model
wanted = ["Bed_A", "Bed_B",
          "A.box", "A.slats", "A.drawer1", "A.drawer2", "A.headboard", "A.mattress",
          "B.box", "B.slats", "B.drawer1", "B.drawer2", "B.drawer3", "B.mattress"]
existing = model.entities.grep(Sketchup::Group).map(&:name)
JSON.generate({
  sketchup_version: Sketchup.version,
  model_path: model.path.to_s,
  top_level_groups: existing.length,
  conflicts: existing & wanted,
})
```

Expected: JSON со строкой `sketchup_version` и `"conflicts":[]`.

Ветвления:
- Ошибка `-32010` («eval_ruby is disabled») → передать пользователю дословно: включить `Plugins → MCP Server → Settings... → Enable Ruby evaluation` (подтвердить security-предупреждение), затем повторить Step 3.
- `conflicts` непуст (остатки прошлого прогона) → СТОП, спросить пользователя: удалить старые группы (по его подтверждению — `model.find_entity_by_id(id).erase!` в одной операции) или работать в новом файле.
- Запомнить `sketchup_version`: если major < 26 — в Task 12 скриншот-инструмент недоступен (см. там fallback).

---

### Task 2: Кровать A — короб (`A.box`, 5 деталей)

**Files:**
- Живая модель SketchUp через `mcp__sketchup__eval_ruby`.
- Reference: спека §5 «Короб».

**Interfaces:**
- Consumes: Task 1 пройден.
- Produces: top-level группа `A.box` с 5 дочерними группами (имена ниже) — Task 11 соберёт её в `Bed_A`.

- [ ] **Step 1: Выполнить чанк**

`mcp__sketchup__eval_ruby`:

```ruby
# --- PRELUDE --- (вставить целиком блок «Прелюдия» из docs/superpowers/plans/2026-07-10-bed-90x200-model.md)

model.start_operation("Bed A: box", true)
begin
  node = model.entities.add_group
  node.name = "A.box"
  e = node.entities
  part.call(e, "A.box.wall_back",      pine, 0,    0,   0,   2056, 18,  420)
  part.call(e, "A.box.wall_end_left",  pine, 0,    18,  0,   18,   920, 420)
  part.call(e, "A.box.wall_end_right", pine, 2038, 18,  0,   18,   920, 420)
  part.call(e, "A.box.rail_front",     pine, 18,   920, 290, 2020, 18,  130)
  part.call(e, "A.box.partition_1",    ply,  1019, 18,  0,   18,   920, 360)
  model.commit_operation
rescue Exception => ex
  model.abort_operation
  raise
end
JSON.generate(report.call(node))
```

- [ ] **Step 2: Сверить bbox с эталоном**

Expected (мм, `[x1,y1,z1,x2,y2,z2]`, допуск ±0.2):

| Группа | bbox |
|---|---|
| `A.box.wall_back` | [0, 0, 0, 2056, 18, 420] |
| `A.box.wall_end_left` | [0, 18, 0, 18, 938, 420] |
| `A.box.wall_end_right` | [2038, 18, 0, 2056, 938, 420] |
| `A.box.rail_front` | [18, 920, 290, 2038, 938, 420] |
| `A.box.partition_1` | [1019, 18, 0, 1037, 938, 360] |
| `A.box` (узел) | [0, 0, 0, 2056, 938, 420] |

- [ ] **Step 3: При расхождении — откат и повтор**

Вызвать `mcp__sketchup__undo` (один раз — откатывает весь чанк), исправить координаты в скрипте, повторить Step 1–2.

---

### Task 3: Кровать A — настил (`A.slats`, 2 бруска + 13 ламелей)

**Files:**
- Живая модель SketchUp через `mcp__sketchup__eval_ruby`.
- Reference: спека §5 «Настил».

**Interfaces:**
- Consumes: Task 1 пройден (от Task 2 геометрически не зависит).
- Produces: top-level группа `A.slats` (15 детей) для Task 11.

- [ ] **Step 1: Выполнить чанк**

`mcp__sketchup__eval_ruby`:

```ruby
# --- PRELUDE --- (вставить целиком блок «Прелюдия» из docs/superpowers/plans/2026-07-10-bed-90x200-model.md)

model.start_operation("Bed A: slats", true)
begin
  node = model.entities.add_group
  node.name = "A.slats"
  e = node.entities
  part.call(e, "A.slats.cleat_back",  pine, 18, 18,  320, 2020, 40, 40)
  part.call(e, "A.slats.cleat_front", pine, 18, 880, 320, 2020, 40, 40)
  pitch = 1880.0 / 12   # 13 ламелей 90 мм: крайние в 25 мм от торцевых стенок, зазор ~66.7
  13.times do |i|
    x = 43.0 + i * pitch
    part.call(e, format("A.slats.slat_%02d", i + 1), pine, x, 23, 360, 90, 910, 20)
  end
  model.commit_operation
rescue Exception => ex
  model.abort_operation
  raise
end
JSON.generate(report.call(node))
```

- [ ] **Step 2: Сверить bbox с эталоном**

Expected: бруски `A.slats.cleat_back` [18, 18, 320, 2038, 58, 360], `A.slats.cleat_front` [18, 880, 320, 2038, 920, 360]. Ламели — все Y 23–933, Z 360–380, X начала (шаг 156.667):

| # | X | # | X | # | X |
|---|---|---|---|---|---|
| 01 | 43.0–133.0 | 06 | 826.3–916.3 | 11 | 1609.7–1699.7 |
| 02 | 199.7–289.7 | 07 | 983.0–1073.0 | 12 | 1766.3–1856.3 |
| 03 | 356.3–446.3 | 08 | 1139.7–1229.7 | 13 | 1923.0–2013.0 |
| 04 | 513.0–603.0 | 09 | 1296.3–1386.3 | | |
| 05 | 669.7–759.7 | 10 | 1453.0–1543.0 | | |

Узел `A.slats`: [18, 18, 320, 2038, 933, 380].

- [ ] **Step 3: При расхождении — `mcp__sketchup__undo`, исправить, повторить Step 1–2**

---

### Task 4: Кровать A — ящик 1, боковые направляющие, ВЫДВИНУТ (`A.drawer1`)

**Files:**
- Живая модель SketchUp через `mcp__sketchup__eval_ruby`.
- Reference: спека §5 «Ящик 1».

**Interfaces:**
- Consumes: Task 1 пройден.
- Produces: top-level группа `A.drawer1` (8 детей) для Task 11.

Коробка и фасад построены в положении «выдвинут на 350 мм» (`dy = 350`); направляющие — статичные тела в монтажном положении на стенках камеры (X 18–1019).

- [ ] **Step 1: Выполнить чанк**

`mcp__sketchup__eval_ruby`:

```ruby
# --- PRELUDE --- (вставить целиком блок «Прелюдия» из docs/superpowers/plans/2026-07-10-bed-90x200-model.md)

dy = 350.0   # выдвижение
model.start_operation("Bed A: drawer 1 (side slides, open)", true)
begin
  node = model.entities.add_group
  node.name = "A.drawer1"
  e = node.entities
  part.call(e, "A.drawer1.slide_L", metal, 18.0,   188, 138, 12.5, 750, 45)
  part.call(e, "A.drawer1.slide_R", metal, 1006.5, 188, 138, 12.5, 750, 45)
  part.call(e, "A.drawer1.side_L",  ply,  30.5,  188 + dy, 40, 12,  750, 240)
  part.call(e, "A.drawer1.side_R",  ply,  994.5, 188 + dy, 40, 12,  750, 240)
  part.call(e, "A.drawer1.front",   ply,  42.5,  926 + dy, 40, 952, 12,  240)
  part.call(e, "A.drawer1.back",    ply,  42.5,  188 + dy, 40, 952, 12,  240)
  part.call(e, "A.drawer1.bottom",  ply,  37.5,  194 + dy, 53, 962, 736, 6)
  part.call(e, "A.drawer1.facade",  pine, 4.0,   938 + dy, 16, 1022, 18, 270)
  model.commit_operation
rescue Exception => ex
  model.abort_operation
  raise
end
JSON.generate(report.call(node))
```

- [ ] **Step 2: Сверить bbox с эталоном**

| Группа | bbox |
|---|---|
| `A.drawer1.slide_L` | [18, 188, 138, 30.5, 938, 183] |
| `A.drawer1.slide_R` | [1006.5, 188, 138, 1019, 938, 183] |
| `A.drawer1.side_L` | [30.5, 538, 40, 42.5, 1288, 280] |
| `A.drawer1.side_R` | [994.5, 538, 40, 1006.5, 1288, 280] |
| `A.drawer1.front` | [42.5, 1276, 40, 994.5, 1288, 280] |
| `A.drawer1.back` | [42.5, 538, 40, 994.5, 550, 280] |
| `A.drawer1.bottom` | [37.5, 544, 53, 999.5, 1280, 59] |
| `A.drawer1.facade` | [4, 1288, 16, 1026, 1306, 286] |
| `A.drawer1` (узел) | [4, 188, 16, 1026, 1306, 286] |

- [ ] **Step 3: При расхождении — `mcp__sketchup__undo`, исправить, повторить Step 1–2**

---

### Task 5: Кровать A — ящик 2, скрытый монтаж, закрыт (`A.drawer2`)

**Files:**
- Живая модель SketchUp через `mcp__sketchup__eval_ruby`.
- Reference: спека §5 «Ящик 2».

**Interfaces:**
- Consumes: Task 1 пройден.
- Produces: top-level группа `A.drawer2` (8 детей) для Task 11.

Камера X 1037–2038. Ящик шире (зазор по 5 мм на сторону), «сидит» на двух рельсах 750×40×15 под дном (упрощённые тела скрытых направляющих, Z 25–40).

- [ ] **Step 1: Выполнить чанк**

`mcp__sketchup__eval_ruby`:

```ruby
# --- PRELUDE --- (вставить целиком блок «Прелюдия» из docs/superpowers/plans/2026-07-10-bed-90x200-model.md)

model.start_operation("Bed A: drawer 2 (undermount, closed)", true)
begin
  node = model.entities.add_group
  node.name = "A.drawer2"
  e = node.entities
  part.call(e, "A.drawer2.slide_L", metal, 1042, 188, 25, 40, 750, 15)
  part.call(e, "A.drawer2.slide_R", metal, 1993, 188, 25, 40, 750, 15)
  part.call(e, "A.drawer2.side_L",  ply,  1042, 188, 40, 12,  750, 240)
  part.call(e, "A.drawer2.side_R",  ply,  2021, 188, 40, 12,  750, 240)
  part.call(e, "A.drawer2.front",   ply,  1054, 926, 40, 967, 12,  240)
  part.call(e, "A.drawer2.back",    ply,  1054, 188, 40, 967, 12,  240)
  part.call(e, "A.drawer2.bottom",  ply,  1049, 194, 53, 977, 736, 6)
  part.call(e, "A.drawer2.facade",  pine, 1030, 938, 16, 1022, 18, 270)
  model.commit_operation
rescue Exception => ex
  model.abort_operation
  raise
end
JSON.generate(report.call(node))
```

- [ ] **Step 2: Сверить bbox с эталоном**

| Группа | bbox |
|---|---|
| `A.drawer2.slide_L` | [1042, 188, 25, 1082, 938, 40] |
| `A.drawer2.slide_R` | [1993, 188, 25, 2033, 938, 40] |
| `A.drawer2.side_L` | [1042, 188, 40, 1054, 938, 280] |
| `A.drawer2.side_R` | [2021, 188, 40, 2033, 938, 280] |
| `A.drawer2.front` | [1054, 926, 40, 2021, 938, 280] |
| `A.drawer2.back` | [1054, 188, 40, 2021, 200, 280] |
| `A.drawer2.bottom` | [1049, 194, 53, 2026, 930, 59] |
| `A.drawer2.facade` | [1030, 938, 16, 2052, 956, 286] |
| `A.drawer2` (узел) | [1030, 188, 16, 2052, 956, 286] |

- [ ] **Step 3: При расхождении — `mcp__sketchup__undo`, исправить, повторить Step 1–2**

---

### Task 6: Кровать A — изголовье и матрас (`A.headboard`, `A.mattress`)

**Files:**
- Живая модель SketchUp через `mcp__sketchup__eval_ruby`.
- Reference: спека §5 «Изголовье и матрас».

**Interfaces:**
- Consumes: Task 1 пройден.
- Produces: две top-level группы `A.headboard`, `A.mattress` (одиночные детали, без узла-обёртки) для Task 11.

- [ ] **Step 1: Выполнить чанк**

`mcp__sketchup__eval_ruby`:

```ruby
# --- PRELUDE --- (вставить целиком блок «Прелюдия» из docs/superpowers/plans/2026-07-10-bed-90x200-model.md)

model.start_operation("Bed A: headboard + mattress", true)
begin
  hb = part.call(model.entities, "A.headboard", pine,  -18, 0,  0,   18,   956, 900)
  mt = part.call(model.entities, "A.mattress",  mgray, 28,  28, 380, 2000, 900, 200)
  model.commit_operation
rescue Exception => ex
  model.abort_operation
  raise
end
JSON.generate({ hb.name => gbox.call(hb), mt.name => gbox.call(mt) })
```

- [ ] **Step 2: Сверить bbox с эталоном**

| Группа | bbox |
|---|---|
| `A.headboard` | [-18, 0, 0, 0, 956, 900] |
| `A.mattress` | [28, 28, 380, 2028, 928, 580] |

- [ ] **Step 3: При расхождении — `mcp__sketchup__undo`, исправить, повторить Step 1–2**

---

### Task 7: Кровать B — короб (`B.box`, 6 деталей)

**Files:**
- Живая модель SketchUp через `mcp__sketchup__eval_ruby`.
- Reference: спека §6.

**Interfaces:**
- Consumes: Task 1 пройден.
- Produces: top-level группа `B.box` (6 детей) для Task 11.

Кровать B — та же геометрия со сдвигом `bx = 2656` по X; две перегородки (камеры 661/661/662).

- [ ] **Step 1: Выполнить чанк**

`mcp__sketchup__eval_ruby`:

```ruby
# --- PRELUDE --- (вставить целиком блок «Прелюдия» из docs/superpowers/plans/2026-07-10-bed-90x200-model.md)

bx = 2656.0
model.start_operation("Bed B: box", true)
begin
  node = model.entities.add_group
  node.name = "B.box"
  e = node.entities
  part.call(e, "B.box.wall_back",      pine, bx + 0,    0,   0,   2056, 18,  420)
  part.call(e, "B.box.wall_end_left",  pine, bx + 0,    18,  0,   18,   920, 420)
  part.call(e, "B.box.wall_end_right", pine, bx + 2038, 18,  0,   18,   920, 420)
  part.call(e, "B.box.rail_front",     pine, bx + 18,   920, 290, 2020, 18,  130)
  part.call(e, "B.box.partition_1",    ply,  bx + 679,  18,  0,   18,   920, 360)
  part.call(e, "B.box.partition_2",    ply,  bx + 1358, 18,  0,   18,   920, 360)
  model.commit_operation
rescue Exception => ex
  model.abort_operation
  raise
end
JSON.generate(report.call(node))
```

- [ ] **Step 2: Сверить bbox с эталоном**

| Группа | bbox |
|---|---|
| `B.box.wall_back` | [2656, 0, 0, 4712, 18, 420] |
| `B.box.wall_end_left` | [2656, 18, 0, 2674, 938, 420] |
| `B.box.wall_end_right` | [4694, 18, 0, 4712, 938, 420] |
| `B.box.rail_front` | [2674, 920, 290, 4694, 938, 420] |
| `B.box.partition_1` | [3335, 18, 0, 3353, 938, 360] |
| `B.box.partition_2` | [4014, 18, 0, 4032, 938, 360] |
| `B.box` (узел) | [2656, 0, 0, 4712, 938, 420] |

- [ ] **Step 3: При расхождении — `mcp__sketchup__undo`, исправить, повторить Step 1–2**

---

### Task 8: Кровать B — настил (`B.slats`)

**Files:**
- Живая модель SketchUp через `mcp__sketchup__eval_ruby`.
- Reference: спека §6 (настил идентичен A со сдвигом +2656).

**Interfaces:**
- Consumes: Task 1 пройден.
- Produces: top-level группа `B.slats` (15 детей) для Task 11.

- [ ] **Step 1: Выполнить чанк**

`mcp__sketchup__eval_ruby`:

```ruby
# --- PRELUDE --- (вставить целиком блок «Прелюдия» из docs/superpowers/plans/2026-07-10-bed-90x200-model.md)

bx = 2656.0
model.start_operation("Bed B: slats", true)
begin
  node = model.entities.add_group
  node.name = "B.slats"
  e = node.entities
  part.call(e, "B.slats.cleat_back",  pine, bx + 18, 18,  320, 2020, 40, 40)
  part.call(e, "B.slats.cleat_front", pine, bx + 18, 880, 320, 2020, 40, 40)
  pitch = 1880.0 / 12
  13.times do |i|
    x = bx + 43.0 + i * pitch
    part.call(e, format("B.slats.slat_%02d", i + 1), pine, x, 23, 360, 90, 910, 20)
  end
  model.commit_operation
rescue Exception => ex
  model.abort_operation
  raise
end
JSON.generate(report.call(node))
```

- [ ] **Step 2: Сверить bbox с эталоном**

Expected: `B.slats.cleat_back` [2674, 18, 320, 4694, 58, 360]; `B.slats.cleat_front` [2674, 880, 320, 4694, 920, 360]; ламели Y 23–933, Z 360–380, X = значения из Task 3 + 2656: 2699.0, 2855.7, 3012.3, 3169.0, 3325.7, 3482.3, 3639.0, 3795.7, 3952.3, 4109.0, 4265.7, 4422.3, 4579.0 (каждая +90). Узел `B.slats`: [2674, 18, 320, 4694, 933, 380].

- [ ] **Step 3: При расхождении — `mcp__sketchup__undo`, исправить, повторить Step 1–2**

---

### Task 9: Кровать B — три ящика, боковые направляющие, средний выдвинут (`B.drawer1..3`)

**Files:**
- Живая модель SketchUp через `mcp__sketchup__eval_ruby`.
- Reference: спека §6.

**Interfaces:**
- Consumes: Task 1 пройден.
- Produces: top-level группы `B.drawer1`, `B.drawer2`, `B.drawer3` (по 8 детей) для Task 11.

Камеры (мировые X): 2674–3335, 3353–4014, 4032–4694. Коробки 636 мм; в камере 3 (662 мм) справа остаётся 1 мм воздуха между коробкой и телом направляющей — это честное отражение спеки (при постройке — подкладка 1 мм либо зазор регулировки), НЕ ошибка модели.

- [ ] **Step 1: Выполнить чанк**

`mcp__sketchup__eval_ruby`:

```ruby
# --- PRELUDE --- (вставить целиком блок «Прелюдия» из docs/superpowers/plans/2026-07-10-bed-90x200-model.md)

bx = 2656.0
cams = [
  { n: 1, x1: bx + 18.0,   x2: bx + 679.0,  dy: 0.0,   fx: bx + 4.0 },
  { n: 2, x1: bx + 697.0,  x2: bx + 1358.0, dy: 350.0, fx: bx + 688.0 },
  { n: 3, x1: bx + 1376.0, x2: bx + 2038.0, dy: 0.0,   fx: bx + 1372.0 },
]
nodes = []
model.start_operation("Bed B: drawers", true)
begin
  cams.each do |c|
    node = model.entities.add_group
    node.name = "B.drawer#{c[:n]}"
    e = node.entities
    p  = node.name
    x1 = c[:x1] + 12.5   # левый край коробки (зазор боковой направляющей)
    dy = c[:dy]
    part.call(e, "#{p}.slide_L", metal, c[:x1],        188, 138, 12.5, 750, 45)
    part.call(e, "#{p}.slide_R", metal, c[:x2] - 12.5, 188, 138, 12.5, 750, 45)
    part.call(e, "#{p}.side_L",  ply,  x1,       188 + dy, 40, 12,  750, 240)
    part.call(e, "#{p}.side_R",  ply,  x1 + 624, 188 + dy, 40, 12,  750, 240)
    part.call(e, "#{p}.front",   ply,  x1 + 12,  926 + dy, 40, 612, 12,  240)
    part.call(e, "#{p}.back",    ply,  x1 + 12,  188 + dy, 40, 612, 12,  240)
    part.call(e, "#{p}.bottom",  ply,  x1 + 7,   194 + dy, 53, 622, 736, 6)
    part.call(e, "#{p}.facade",  pine, c[:fx],   938 + dy, 16, 680, 18, 270)
    nodes << node
  end
  model.commit_operation
rescue Exception => ex
  model.abort_operation
  raise
end
JSON.generate(nodes.map { |n| report.call(n) }.reduce(:merge))
```

- [ ] **Step 2: Сверить bbox с эталоном**

`B.drawer1` (закрыт, камера 2674–3335):

| Группа | bbox |
|---|---|
| `B.drawer1.slide_L` | [2674, 188, 138, 2686.5, 938, 183] |
| `B.drawer1.slide_R` | [3322.5, 188, 138, 3335, 938, 183] |
| `B.drawer1.side_L` | [2686.5, 188, 40, 2698.5, 938, 280] |
| `B.drawer1.side_R` | [3310.5, 188, 40, 3322.5, 938, 280] |
| `B.drawer1.front` | [2698.5, 926, 40, 3310.5, 938, 280] |
| `B.drawer1.back` | [2698.5, 188, 40, 3310.5, 200, 280] |
| `B.drawer1.bottom` | [2693.5, 194, 53, 3315.5, 930, 59] |
| `B.drawer1.facade` | [2660, 938, 16, 3340, 956, 286] |
| `B.drawer1` (узел) | [2660, 188, 16, 3340, 956, 286] |

`B.drawer2` (ВЫДВИНУТ +350, камера 3353–4014):

| Группа | bbox |
|---|---|
| `B.drawer2.slide_L` | [3353, 188, 138, 3365.5, 938, 183] |
| `B.drawer2.slide_R` | [4001.5, 188, 138, 4014, 938, 183] |
| `B.drawer2.side_L` | [3365.5, 538, 40, 3377.5, 1288, 280] |
| `B.drawer2.side_R` | [3989.5, 538, 40, 4001.5, 1288, 280] |
| `B.drawer2.front` | [3377.5, 1276, 40, 3989.5, 1288, 280] |
| `B.drawer2.back` | [3377.5, 538, 40, 3989.5, 550, 280] |
| `B.drawer2.bottom` | [3372.5, 544, 53, 3994.5, 1280, 59] |
| `B.drawer2.facade` | [3344, 1288, 16, 4024, 1306, 286] |
| `B.drawer2` (узел) | [3344, 188, 16, 4024, 1306, 286] |

`B.drawer3` (закрыт, камера 4032–4694):

| Группа | bbox |
|---|---|
| `B.drawer3.slide_L` | [4032, 188, 138, 4044.5, 938, 183] |
| `B.drawer3.slide_R` | [4681.5, 188, 138, 4694, 938, 183] |
| `B.drawer3.side_L` | [4044.5, 188, 40, 4056.5, 938, 280] |
| `B.drawer3.side_R` | [4668.5, 188, 40, 4680.5, 938, 280] |
| `B.drawer3.front` | [4056.5, 926, 40, 4668.5, 938, 280] |
| `B.drawer3.back` | [4056.5, 188, 40, 4668.5, 200, 280] |
| `B.drawer3.bottom` | [4051.5, 194, 53, 4673.5, 930, 59] |
| `B.drawer3.facade` | [4028, 938, 16, 4708, 956, 286] |
| `B.drawer3` (узел) | [4028, 188, 16, 4708, 956, 286] |

- [ ] **Step 3: При расхождении — `mcp__sketchup__undo`, исправить, повторить Step 1–2**

---

### Task 10: Кровать B — матрас (`B.mattress`)

**Files:**
- Живая модель SketchUp через `mcp__sketchup__eval_ruby`.
- Reference: спека §6.

**Interfaces:**
- Consumes: Task 1 пройден.
- Produces: top-level группа `B.mattress` для Task 11.

- [ ] **Step 1: Выполнить чанк**

`mcp__sketchup__eval_ruby`:

```ruby
# --- PRELUDE --- (вставить целиком блок «Прелюдия» из docs/superpowers/plans/2026-07-10-bed-90x200-model.md)

model.start_operation("Bed B: mattress", true)
begin
  mt = part.call(model.entities, "B.mattress", mgray, 2684, 28, 380, 2000, 900, 200)
  model.commit_operation
rescue Exception => ex
  model.abort_operation
  raise
end
JSON.generate({ mt.name => gbox.call(mt) })
```

- [ ] **Step 2: Сверить bbox с эталоном**

Expected: `B.mattress` [2684, 28, 380, 4684, 928, 580].

- [ ] **Step 3: При расхождении — `mcp__sketchup__undo`, исправить, повторить Step 1–2**

---

### Task 11: Сборка узлов в `Bed_A` / `Bed_B` и проверка структуры

**Files:**
- Живая модель SketchUp через `mcp__sketchup__eval_ruby`, `mcp__sketchup__list_components`.
- Reference: спека §7.

**Interfaces:**
- Consumes: все top-level узлы из Tasks 2–10 (точные имена в скрипте ниже).
- Produces: две top-level группы `Bed_A`, `Bed_B` — финальная структура модели.

- [ ] **Step 1: Выполнить сборочный чанк**

`mcp__sketchup__eval_ruby` (прелюдия не нужна — геометрия не создаётся):

```ruby
require "json"
mm = 25.4
model = Sketchup.active_model

a_names = ["A.box", "A.slats", "A.drawer1", "A.drawer2", "A.headboard", "A.mattress"]
b_names = ["B.box", "B.slats", "B.drawer1", "B.drawer2", "B.drawer3", "B.mattress"]

wrap = lambda do |bed_name, names|
  tops = model.entities.grep(Sketchup::Group)
  nodes = names.map { |n| tops.find { |g| g.name == n } }
  missing = names.zip(nodes).select { |_, g| g.nil? }.map(&:first)
  raise "missing nodes: #{missing.inspect}" unless missing.empty?
  bed = model.entities.add_group(nodes)
  bed.name = bed_name
  bed
end

model.start_operation("Assemble beds", true)
begin
  bed_a = wrap.call("Bed_A", a_names)
  bed_b = wrap.call("Bed_B", b_names)
  model.commit_operation
rescue Exception => ex
  model.abort_operation
  raise
end

info = lambda do |bed|
  b = bed.bounds
  {
    children: bed.entities.grep(Sketchup::Group).map(&:name).sort,
    bbox: [b.min.x, b.min.y, b.min.z, b.max.x, b.max.y, b.max.z].map { |v| (v * mm).round(1) },
  }
end
JSON.generate({ "Bed_A" => info.call(bed_a), "Bed_B" => info.call(bed_b) })
```

- [ ] **Step 2: Сверить структуру и bbox**

Expected:
- `Bed_A.children` = `["A.box","A.drawer1","A.drawer2","A.headboard","A.mattress","A.slats"]` (сортировка); bbox [-18, 0, 0, 2056, 1306, 900].
- `Bed_B.children` = `["B.box","B.drawer1","B.drawer2","B.drawer3","B.mattress","B.slats"]`; bbox [2656, 0, 0, 4712, 1306, 580].

- [ ] **Step 3: Кросс-проверка через штатный инструмент**

Вызвать `mcp__sketchup__list_components` с `recursive=true, max_depth=2, limit=50`.
Expected: на верхнем уровне присутствуют `Bed_A` и `Bed_B` с дочерними узлами из Step 2 (плюс возможные сущности пользователя, если модель была непустой).

- [ ] **Step 4: При провале Step 1 (raise "missing nodes")**

Не откатывать назад весь прогон: выяснить, какой узел отсутствует (см. сообщение ошибки), вернуться к соответствующему таску 2–10, выполнить его, затем повторить Task 11. Если чанк упал ПОСЛЕ создания `Bed_A`, но до `Bed_B` — операция уже отменена целиком в rescue (`abort_operation`), повторять Task 11 с начала безопасно.

---

### Task 12: Изометрический скриншот и визуальная приёмка

**Files:**
- Живая модель SketchUp через `mcp__sketchup__get_viewport_screenshot`.
- Reference: спека §8, критерий готовности.

**Interfaces:**
- Consumes: Bed_A/Bed_B из Task 11; `sketchup_version` из Task 1.

- [ ] **Step 1: Снять изометрию**

Если major-версия SketchUp из Task 1 ≥ 26 — вызвать `mcp__sketchup__get_viewport_screenshot` с аргументами: `view_preset="iso"`, `zoom_extents=true`, `style="shaded"`, `max_size=1200`.
Если < 26 — инструмент не поддерживается: пропустить вызов и попросить пользователя повернуть вид вручную (Camera → Standard Views → Iso, Zoom Extents) и оценить модель по чек-листу Step 2.

Expected: PNG-изображение + JSON-метаданные без ошибок.

- [ ] **Step 2: Визуальный чек-лист по скриншоту**

- Две кровати рядом (зазор 600 мм), обе фасадами в одну сторону (+Y).
- У левой (A) — изголовье на левом торце, 2 фасада; у правой (B) — 3 фасада, изголовья нет.
- Левый ящик кровати A и средний кровати B выдвинуты; в их проёмах видны металлик-тела направляющих: у A — вертикальные полосы на стенках камеры (боковые), у ящика A.drawer2 при желании видно рельсы под дном (скрытые), у B — боковые.
- Матрасы (серые) утоплены в короба; нет торчащих или провалившихся деталей, нет щелей между стенками короба.
- Фасады с равномерными зазорами 4 мм (визуально — тонкие линии).

- [ ] **Step 3: Показать скриншот пользователю и зафиксировать приёмку**

Передать пользователю скриншот + суммарную таблицу узлов (из Task 11). Спросить, принимает ли модель. Мелкие правки (перекраска, сдвиг кроватей, открыть/закрыть ящики) — отдельными `eval_ruby`-чанками с тем же паттерном операции.

- [ ] **Step 4 (опционально, по желанию пользователя): сохранить копию сцены**

`mcp__sketchup__export_scene` с `format="skp"`. Файл создаётся на машине, где работает SketchUp; путь вернётся в ответе — сообщить его пользователю. Файл модели пользователя НЕ пересохранять (`model.save` запрещён Global Constraints).

---

## Примечание для исполнителя

- Порядок Tasks 2–10 взаимонезависим геометрически (узлы не пересекаются), но выполнять их по порядку — так проще откатывать хвост при ошибке (`undo` откатывает последнюю операцию).
- Каждый вызов `eval_ruby` проходит per-call review в MCP-клиенте — это ожидаемо; не пытаться объединять все чанки в один вызов.
- Если пользователь работает в модели с существующей геометрией — она не мешает: все наши группы имеют уникальные имена (проверено в Task 1).
