# Review Iteration 1 — 2026-07-03

## Источник

- Design: `docs/superpowers/specs/2026-07-02-deep-review-batch2-design.md`
- Plan: `docs/superpowers/plans/2026-07-02-deep-review-batch2.md`
- Review agents: builtin claude (Fable), codex (gpt-5.5 xhigh), ext-claude ×5 — alibaba/qwen ✅, deepseek/v4-pro ✅, zai/glm ⚠ частично (обрыв стрима, факт-чекинг из лога), ollama/minimax ⚠ частично (контекстный лимит, выжимка из лога), ollama/kimi ❌ (3 обрыва стрима, без результата)
- Merged output: `docs/superpowers/specs/2026-07-02-deep-review-batch2-review-merged-iter-1.md`
- Разбор и дедупликация: агент review-discussion (PARSED_ISSUES v2 — 48 карточек + 5 опровергнутых)
- Особенности прогона: merge выполнен по указанию пользователя без ожидания kimi; лимит Fable в середине прогона потребовал ре-запуска четырёх агентов; результаты glm/deepseek первых прогонов оборваны стримом провайдера, deepseek завершил со второй попытки (watchdog).

## Замечания

### Критические (правки плана до исполнения)

**[P-01] caplog-ассерт даст TypeError + счётчики 5-vs-6 (Task 1 / T-12)** — `r.message % r.args` падает (pytest форматирует record при emit); старых тестов test_config.py 5, не 6.
Источник: codex, rev-claude C2, minimax-7. Статус: Автоисправлено. Действие: `r.getMessage()` ×2; счётчики «10 failed, 6 passed» / «16 passed».

**[P-02] remove_method удалит реальные `def self.`-методы под run_all (Task 11 / T-27)** — модульные методы суть singleton; «снятие стаба» удаляло их насовсем.
Источник: codex, deepseek CI-4. Статус: Автоисправлено. Действие: `with_export_stubs` переписан на save-Method (эталон test_collect_components.rb); обоснование в плане заменено честным; Global Constraints дополнены предупреждением.

**[P-03] Пагинация: полный обход + slice; судьба total (T-07)** — CPU/память Ruby не экономятся, только размер ответа.
Источник: codex. Статус: Обсуждено → оставить как есть (авто после анализа). Ответ: точный total неизбежно требует полного обхода; обход всей модели был нормой хендлера и до пагинации; цель тикета — размер ОТВЕТА — достигнута. Действие: комментарий-обоснование у paginate в плане.

**[P-04] Ruby-валидация limit без верхней границы (T-07)** — контракт 1..500 держался только на Python.
Источник: codex, rev-claude C5. Статус: Автоисправлено. Действие: хелпер `V.optional_int_range`, `LIMIT_MAX = 500`, кейс `limit=501` в Ruby-тест; M-06 влит (пустая строка невалидна через require_string + min_length=1 на Python).

**[P-05] Pydantic-коэрция подрывает «зеркальность»; bool-дыра EntityId (Task 5/13)** — True→1→id «1»; "3"→3.
Источник: codex, qwen Q8. Статус: Обсуждено → принята асимметрия (авто после анализа). Ответ: Python — дружелюбный ранний фильтр для LLM (коэрция намеренна), Ruby — строгая инстанция; слово «зеркальные» уточнено «по границам, не по типам». Действие: EntityId получил strict-int ветку (проба подтвердила дыру: `call(bool True) OK -> int:1`), пин-тест `test_bool_id_rejected`, правка дизайна T-17 и примечание в Task 13.

**[P-06] Backpressure-guard только на входе в фазу чтения (T-13.2)** — один тик мог накачать очередь сильно выше SOFT_MAX.
Источник: codex, rev-claude C4, minimax-14. Статус: Автоисправлено. Действие: guard добавлен внутрь read-цикла drain_one_client (перелёт ≤ один 64 KiB кусок); комментарий о глобальности стопа (fairness — осознанно) и связке констант (M-08); новый дискриминирующий тест `test_flood_stops_reading_mid_drain_once_queue_saturated`.

**[P-07] Pre-handshake свип убивал клиента, дренирующего reject-envelope (T-13.5)** — конфликт с T-13.1.
Источник: codex, minimax-16, qwen Q5. Статус: Автоисправлено. Действие: `next if state.close_after_response` в свипе; новый тест sweep-spares-draining; комментарий: 30 с — hello уходит первым фреймом сразу после connect (WAN-RTT на порядки меньше); DoS-окно до дедлайна отражено.

**[P-08] Units-тест требовал mm у export_scene, докстринг их не содержит (Task 15 / T-05)** — тест был бы красным после «GREEN».
Источник: codex, rev-claude C3 (независимо). Статус: Автоисправлено. Действие: export_scene исключён из units-списка (линейных мм-параметров у него нет).

**[P-09] `-> list[Image | str]` падает на регистрации mcp 1.27 (Task 9 / T-28)** — дубль ×4.
Источник: codex, qwen C2+S5, deepseek SUGG-3, rev-claude S1+Q5. Статус: Автоисправлено (с экспериментальной верификацией). Ответ: проба подтвердила — регистрация падает; `-> list` работает, голый str конвертируется в TextContent автоматически. Действие: план предписывает `-> list:` сразу (без «если споткнётся»); порядок блоков и состав meta зафиксированы.

**[P-10] Регистрация anyOf-схемы EntityId не верифицирована (Task 5 / T-06)**.
Источник: qwen C1+S3. Статус: Автоисправлено (с верификацией). Ответ: проба — схема `anyOf [{integer},{string minLength 1}]` регистрируется корректно. Действие: schema-пин `test_entity_id_schema_exposes_int_and_string`.

**[P-11] Empty-bbox предикат дублирован ×3 и проверял только ось x (Task 8 / T-55)**.
Источник: qwen C3+S2, codex. Статус: Автоисправлено. Действие: единый `Helpers::Geometry.empty_bbox?(bb)` (все три оси) во всех трёх сайтах; тест одноосевой инверсии.

**[P-12] Нет инвентаризации маинтейнерских заметок (Task 15 / T-05)**.
Источник: qwen C4+S1. Статус: Автоисправлено. Действие: Step 0 с grep и списком всех 5 найденных заметок (строки 174, 193, 326–327, 341–342, 377).

**[P-13] Порог polar-chord 0.03 мм — запас 1.18× merge-tolerance (Task 13 / MR-2)**.
Источник: deepseek CI-1 (его числовой пример противоречив — отмечено парсером). Статус: Обсуждено с пользователем (вместе с C-13). Ответ: Вариант A. Действие: `MIN_POLAR_CHORD_MM = 0.04` (~1.6×).

**[P-14] get_viewport_screenshot в _RETRY_SAFE_TOOLS — не read-only по viewport (MR-1)**.
Источник: deepseek CI-2. Статус: Обсуждено → оставить в whitelist (авто после анализа). Ответ: по данным ретрай идемпотентен (restore в ensure до записи ответа); скриншот — крупнейший ответ и главный кандидат на обрыв, исключение убило бы главный кейс MR-1; мерцание не исчезло бы (модель ретраила бы руками). Действие: фраза о возможном мерцании в докстринг (Task 15) + примечание в Task 3 Interfaces.

**[P-15] remove_const/const_set в тестовом хелпере капа (Task 10 / T-13.3)**.
Источник: deepseek CI-3 (риск преувеличен — ensure выполняется и при упавшем ассерте, отмечено парсером). Статус: Обсуждено → принято осознанно (авто после анализа). Ответ: альтернатива (аксессор в проде ради теста) отклонена. Действие: комментарий об ensure-гарантии в хелпер.

**[P-16] Предупреждения о source-guard'ах указывали не туда (Task 8/12)** — describe_entity никем не пинится.
Источник: deepseek CI-5, rev-claude S2. Статус: Автоисправлено. Действие: Task 8 — предупреждение заменено констатацией (grep пуст); Task 12 — добавлен standalone-прогон test_operation_names.rb с честной оценкой риска.

**[P-17] Существующий overflow-тест СТАНЕТ КРАСНЫМ после T-13.3 — план утверждал обратное (Task 10)** — трёхсторонний конфликт ревьюеров разрешён.
Источник: rev-claude C1 (прав), minimax-1 (частично: head-guard в server.rb:331 действительно уже есть; выводы о недискриминации RED и неверной мотивации дизайна — опровергнуты), glm (неправ — «пройдёт под обеими формулами»). Статус: Автоисправлено. Действие: Step 6 переписан — прямое предписание переработки `test_pending_write_overflow_closes_client` (малый head-append перед `near` сохраняет прежнюю границу срабатывания), фиксация в commit message.

**[P-18] Ассерт MR-1 «NOT auto-retried» не совпадает с кодом (Task 3)** — реальный текст «do NOT retry».
Источник: minimax-4 (подтверждено grep: connection.py:290). Статус: Автоисправлено. Действие: ассерт заменён.

### Concerns

**[C-01] Unguarded `module Sketchup`/`Geom` в новых тест-файлах vs Global Constraints.** Источник: codex. Статус: Автоисправлено. Ответ: фактический паттерн проекта — аддитивный реопен (эталон test_collect_components.rb, проверено чтением); guarded-скип чужого скупого стаба не дал бы нужных аксессоров. Действие: Global Constraints переформулированы (guarded ИЛИ аддитивный реопен, с объяснением выбора).

**[C-02] «Untracked prompt-файлы уйдут вместе с git rm» — неверно.** Источник: codex. Статус: Обсуждено → формулировка исправлена, untracked не трогаем (авто после анализа). Действие: дизайн §5 и план «После плана» п.2 — git rm снимает только tracked; prompt-файлы — локальный архив владельца.

**[C-03] concise-формат вырезал layer (T-07)** — дубль ×3. Источник: qwen Q3, deepseek CONCERN-2/Q-2/Q-4, rev-claude Q4. Статус: Обсуждено → добавить layer (авто после анализа: поле дешёвое, сценарий частый, три независимых голоса). Действие: layer в concise-набор (Interfaces, paginate, тест, докстринг).

**[C-04] Монолит Task 10 (5 подфиксов) — разбить на 10a–10e?** Источник: deepseek CONCERN-1+SUGG-1. Статус: Отклонено (обсуждено → оставить как есть). Ответ: атомарность уже обеспечена — каждый подфикс имеет свой RED→GREEN→commit; разбиение = перенумерация задач 11–17 без изменения фактического процесса; риск сам deepseek оценил как умеренный и управляемый.

**[C-05] Тесты Task 15 проверяют форму, не содержание Returns.** Источник: deepseek CONCERN-5, minimax-13. Статус: Автоисправлено. Действие: content-пины Returns топ-5 тулов (`test_returns_lines_pin_top_response_shapes`).

**[C-06] bbox_mm=null — Python/prompts-сторона.** Источник: deepseek SUGG-4, qwen Q6. Статус: Автоисправлено. Действие: констатация в Task 8 Interfaces (Python не валидирует формы ответов — null безопасен по построению); в prompts-тексте null-семантика («treat null as inspect what happened, not as an error»).

**[C-07] Fallback get_component_info при depth>64 — bbox в parent-frame без пометки.** Источник: deepseek SUGG-5. Статус: Автоисправлено. Действие: деградация задокументирована в комментарии метода (Task 7 Step 3f).

**[C-08] Валидация scale не действует на eval_ruby.** Источник: deepseek CONCERN-3. Статус: Автоисправлено. Действие: фраза в докстринг transform_component (Task 15).

**[C-09] FakeAffineZ доказывает логику, не float-арифметику (MR-3).** Источник: deepseek CONCERN-4. Статус: Автоисправлено. Действие: явный комментарий в шапке тест-класса.

**[C-10] Мутации через subtract идут мимо make_unique (T-16)** — boolean_operation/place_mortise. Источник: rev-claude C1(Concern)+Q1. Статус: Обсуждено → граница зафиксирована (авто после анализа). Ответ: subtract не мутирует definition in-place — потребляет входы, создаёт новую группу; sibling-инстансы не затронуты по построению; make_unique на этих путях не нужен. Действие: комментарий-граница в Task 12 Interfaces; фраза в докстринг boolean_operation (Task 15); ручной пункт live-smoke владельца (копия группы + boolean над одной).

**[C-11] ScriptError-тест T-23.3 — дубликат существующего покрытия.** Источник: rev-claude C3(Concern), minimax-11. Статус: Автоисправлено. Действие: дубликат убран (test_dispatch_post_handshake.rb:243 уже пинит arm), остался только StandardError-тест; пометка gap-filling «обязан PASS»; commit message скорректирован.

**[C-12] Task 16 Step 1 порождал дубликат строк README.** Источник: rev-claude C2(Concern). Статус: Автоисправлено. Действие: только удаление README:137-138, вставка отменена (корректные строки уже стоят на :135-136).

**[C-13] Floor 1.0 мм запрещает шпон/лист 0.5–0.8 мм (MR-2).** Источник: rev-claude Q2. Статус: Обсуждено с пользователем (вместе с P-13). Ответ: **Вариант A — per-type floor**. Действие: `MIN_DIMENSION_MM_BOX = 0.1`, `MIN_DIMENSION_MM_CURVED = 1.0`, `validate_min_dimensions!(dims_mm, type)`; Python `Field(ge=0.1)` (абсолютный floor, per-type — Ruby-инстанция); тесты per-type; докстринг и prompts обновлены; дизайн MR-2 дополнен решением.

**[C-14] MR-3: легенда фейка не согласована с трансформацией.** Источник: rev-claude S4d (подтверждён алгеброй); аспект minimax-6 («flip не уронит тесты») ОПРОВЕРГНУТ допроверкой парсера по joints.rb → F-06. Статус: Автоисправлено. Действие: bounds исправлены на x 26..30 (R90+dx30 для локального 0..4; равноценная альтернатива dx=34 — не выбрана); flip-инструкция дополнена разъяснением эквивалентности (`acc ≡ acc.compose(identity)`) и СТОП-правилом при неожидаемо зелёном прогоне.

### Minor

**[M-02]** LOOKUP_MAX_DEPTH уже существует — указано в Task 7 (Автоисправлено). **[M-03]** полный пин obj-хеша — предписан в тесте Task 11 (Автоисправлено). **[M-04]** явный сигнал `raise "validation passed"` + save-Method вместо NoMethodError в dovetail-тесте (Автоисправлено). **[M-05]** фраза «int 1 проходит Field(ge=…)» (Автоисправлено). **[M-06]** пустая строка name/layer → invalid: влит в P-04 (require_string уже отклоняет; Python min_length=1 добавлен) (Автоисправлено). **[M-07]** семантика copy-paste Group уточнена в комментарии mutable_entity_collection (Автоисправлено). **[M-08]** обоснование констант 50/256 + связка с READ_MAX_ITERATIONS/TIMER_INTERVAL — комментарий в Task 10 (Автоисправлено). **[M-10]** перепрогнать базлайны на старте исполнения — в Global Constraints (Автоисправлено). **[M-11]** напоминание о leak-подстроках в правилах Task 15 (Автоисправлено). **[M-12]** пункт про optional name в prompts §3 (Автоисправлено). **[M-13]** потерянное дизайн-требование T-22 — happy-path тест полной transform-комбинации добавлен в Task 4 (Автоисправлено). **[M-14]** ориентиры пересчитаны: Python 136→176, Ruby ~413 (354 + ~59) — фактический прогон авторитетен (Автоисправлено). **[M-15]** пакет якорей: «10 тулов / 14 id-параметров» — оставлена таблица (сама на 10 строк; заголовок «11 тулов» не найден в актуальном тексте — не потребовалось), cur_edges → find_current_edge_spec, переименование test_too_new_*, строка 97 — «~106» оставлен как приблизительный (Автоисправлено в валидных частях). **[M-16]** комментарий, почему EHOSTUNREACH (Автоисправлено). **[M-17]** цена повторного 43 MiB захвата — в докстринге _StaleSocketError (Автоисправлено).

### Отклонённые / ложные

**[M-01]** dispatch_conn возвращает один conn — гипотетический риск будущих тестов, текущие не затронуты (Отклонено). **[F-01]** T-21 flip без reload не упадёт (minimax-3) — ложное: flip идёт в новом процессе (Отклонено). **[F-02]** CountingSocket «голый super упадёт» (minimax-5) — ложное: голый super пересылает и kwargs (Отклонено). **[F-03]** T-19 RED не дискриминирует (minimax-2) — ложное в главном: standalone-файл плана сам не грузит core/logger и делает require "logger" — дискриминация есть; недискриминация под run_all планом признана явно (Отклонено). **[F-04]** dae/stl «на веру» (minimax-12) — план Step 2c прямо пинит их тестом; валидный остаток учтён в M-03 (Отклонено). **[F-05]** «дизайн ссылается на пины place_tenon» (minimax-10) — в дизайне таких ссылок нет, grep пуст (Отклонено). **[F-06]** «flip MR-3 (compose→acc) не уронит тесты» (minimax-6) — ложное: прототип строится в МИРОВЫХ координатах от bounds.center (joints.rb:298-316), компенсация T⁻¹ идёт через add_instance; при flip T_board применяется к уже-мировым точкам → y уходит за допуск → оба теста падают, как план и требует (допроверка парсера по коду; Отклонено). Части minimax-1 («RED не дискриминирует», «мотивация дизайна неверна») опровергнуты внутри P-17.

## Изменения в документах

| Файл | Изменение |
|------|-----------|
| docs/superpowers/plans/2026-07-02-deep-review-batch2.md | 34 авто-фикса (коммит 7dd2539: caplog/счётчики, save-Method ×3, optional_int_range+LIMIT_MAX, backpressure-guard внутрь цикла+тест, sweep-skip+тест, units-тест без export_scene, «-> list:» по пробе, schema-пин EntityId, empty_bbox? ×3+тест, инвентаризация заметок, source-guard констатации, переработка overflow-теста, «do NOT retry», content-пины Returns, C-11 дубль убран, C-12 README, C-14 легенда+flip, M-пакет) + правки спорной фазы (strict-EntityId+bool-тест, per-type пороги MR-2, границы T-16/C-02, layer в concise, P-14/P-15 примечания, каскад счётчиков до 176/413) |
| docs/superpowers/specs/2026-07-02-deep-review-batch2-design.md | T-17: «зеркальность по границам, не по типам»; MR-2: per-type пороги (решение пользователя); §5 T-25: судьба untracked prompt-файлов исправлена |

## Статистика

- Всего замечаний (карточек после дедупликации): 48 + 5 опровергнутых парсером
- Автоисправлено (без обсуждения): 35
- Авто-применено после анализа: 6 (P-03, P-05, C-02, C-03, C-10, P-14, P-15 → из них P-14/P-15 — примечания; счёт по карточкам: 7)
- Обсуждено с пользователем: 2 (P-13 + C-13 — один вопрос, выбран Вариант A: per-type floor 0.1/1.0 мм, chord 0.04)
- Отклонено: 9 (C-04, M-01, F-01…F-06, плюс части minimax-1 внутри P-17)
- Повторов (автоответ): 0 (итерация 1)
- Пользователь сказал «стоп»: Нет
- Агенты: builtin claude, codex, alibaba/qwen, deepseek/v4-pro, zai/glm (частично), ollama/minimax (частично); ollama/kimi — без результата
